# 自动化域（domain/automation）夜间防卡死自测任务书

> 文档版本：v1.0 ｜ 编制日期：2026-08-09 ｜ 编制角色：集成代理代拟，执行者 = 自动化域执行 agent
> 执行方式：**通宵全自动**。本文件交给一个独立 agent 线程，在 `E:\My_project\QT\Motor_Antomation-wt\automation` 内循环执行「审计 → 复现 → 修复 → 压力测试 → 门禁验证」，**直到 §11 验收矩阵全绿为止**。除 C 盘文件操作外不需要人工介入。
> 共享规格：本任务书的通用机制（卡死判定、看门狗、报告格式、合并回 master 的规则）见 master 分支 `docs/夜间防卡死自测_共享执行规格.md`（若本 worktree 尚无该文件，先执行 Phase 0 的 `git merge master` 拉入）。

---

## 0. 任务目标与零容忍标准

1. **零卡死**：自动化域所有可视化控件，在任何用户交互序列下不得出现界面冻结 / 无响应 / 假死（判定标准见共享规格 §2：UI 线程事件循环单次阻塞 > 300ms 即视为卡死）。
2. **零崩溃**：不得出现段错误、堆损坏、未捕获异常导致的进程退出。
3. **禁止打补丁式修复**：用户已反复经历"修一个又冒一个"。本轮要求先建立全局缺陷清单，**同一类根因必须用同一个方案一次性修完**，并为每一类根因留下回归测试。
4. 本任务书覆盖自动化域全部名下文件（CLAUDE.md §2）：`src/automation/**`、`src/parameter/**`、`src/scripting/**`、`src/plugin/**`、`src/ui/AutomationWidget`、`FlowCanvas`、`NodeLibraryPanel`、`NodeParamPanel`、`VariableEditorPanel`。

---

## 1. 已知症状与历史根因（开工前必读）

### 1.1 用户当前反馈的症状（最高优先级）

| 编号 | 操作序列（精确复现步骤） | 现象 |
|---|---|---|
| SYM-1 | 在参数控件中输入数据 → **立刻按 Enter** → **100ms 内开始拖动控件** | 界面卡死 |
| SYM-2 | 输入数据后不按 Enter 直接点别处 / 拖动 | 偶发卡死 |
| SYM-3 | 快速连续双击节点库添加节点 | 卡死或崩溃（历史上已出现过，见 BUG-001） |

### 1.2 历史 Bug 档案（master 的 `motor_antomation_Bug_Report.md`，同区域根因）

- **BUG-001**：双击添加节点 / 编辑节点参数 → `NodeParamPanel::buildForm` 双重释放 → 段错误（已修 `2de36a5`，用 takeRow 替代 removeRow）。
- **BUG-004**：输入参数后按回车 / 编辑 If·Assert 节点 → `buildForm`"参数行包删除按钮"段悬垂指针 + 堆损坏 → 卡死（已修 `f44bac9`）。
- **教训**：BUG-004 是 BUG-001 同区域修得不彻底导致的二次爆发。**本轮必须把 `buildForm` 全路径做一次彻底的生命周期审计，而不是继续逐症状修补。**

### 1.3 待排查的高危根因模式（审计时按此清单逐项过）

| 模式 | 说明 | 本域高发位置 |
|---|---|---|
| P1 悬垂指针 / use-after-free | `QFormLayout::removeRow/takeRow`、`deleteLater` 与同步访问混用、保存裸指针跨事件循环 | `NodeParamPanel::buildForm`、节点切换路径 |
| P2 事件重入 | 槽函数里 `exec()` 模态框 / 槽函数里触发会再次发信号的操作（setText→editingFinished→重建表单） | 输入框 Enter 路径、`QInputDialog::getText` |
| P3 信号槽循环 | A→B→A 无终止条件的 emit 链 | 参数面板 ↔ 引擎回写 |
| P4 拖拽/绘制事件风暴 | 拖动时每个 mouseMove 触发整表重建 / 全量重绘 / 重新布局 | `FlowCanvas`（QGraphicsView 拖动、连线拖拽）、表格控件 |
| P5 UI 线程阻塞 | 槽函数里做文件 IO、同步等待、大循环 | 加载/保存流程图 JSON、报告生成 |
| P6 未加保护的动态增删 | 拖动/删除进行时容器正在被其他事件修改（迭代器失效） | 节点/连线/参数行的增删 |

---

## 2. 执行规则（强制）

1. **四阶段顺序不可跳过**：Phase 1 审计 → Phase 2 复现用例 → Phase 3 统一修复 → Phase 4 仿真压力测试。每完成一个 Phase 必须在执行日志中输出阶段报告再继续。
2. **只改本域名下文件**（§0.4 清单）。发现根因落在 master-owned 红线文件（`mainwindow.*`、`src/core/**`、`src/databus/**` 等）时：**不得修改**，在报告中标注「需集成代理处理」并给出建议。
3. **证据优先**：所有"已修复 / 已验证"的声明必须附命令输出（ctest、gate 日志），不采信口头结论（CLAUDE.md §10）。
4. **每处修复必须配套回归测试**，且测试必须先能复现缺陷（修复前红 / 修复后绿），否则视为无效修复。
5. 提交规范：前缀 `[自动化]`，提交信息写清根因类别（P1-P6）与影响控件。

---

## 3. Phase 0 — 环境准备与基线（先做，不得跳过）

1. `cd E:\My_project\QT\Motor_Antomation-wt\automation`，`git status` 确认工作区；如有本任务之外的未提交改动，先按归属提交或 stash。
2. **同步 master**：`git merge master`。master 上已有门禁基础设施（`scripts\gate.bat`、静态检查、CI、测试修复提交），域分支落后多个提交，**必须先合并再开工**。冲突只允许出现在 master-owned 文件 → 按 CLAUDE.md §4.4 上报，不自行改写。
3. 构建并跑一遍全量测试建立基线：
   ```
   scripts\gate.bat                （若本 worktree 无此文件，用主 checkout：E:\My_project\QT\Motor_Antomation\scripts\gate.bat，工作目录设为本 worktree 的 motor_antomation\）
   ctest --test-dir build\ci-tests --output-on-failure
   ```
4. 记录基线：哪些测试当前通过 / 失败 / 超时，写入 `reports/nightly_baseline_automation.md`。
5. 环境设置：所有 UI 测试一律 `QT_QPA_PLATFORM=offscreen`；无硬件，数据一律走 `DeviceSimulator` 模拟管线。

**Phase 0 验收**：基线报告产出；gate.bat 能跑出明确结果（哪怕有失败，也要记录失败项）。

---

## 4. Phase 1 — 只读审计（不改代码）

对本域全部控件的事件处理链做一次系统审计，覆盖以下交互矩阵：

| 控件 | 必须审计的交互 |
|---|---|
| `NodeParamPanel` | 每个输入控件键入 / Enter / Tab 失焦 / 切换节点 / 删除参数行 / 添加参数对话框确认 |
| `FlowCanvas` | 节点拖动（含高速连续 mouseMove）、连线拖拽、框选、右键菜单、滚轮缩放、双击节点 |
| `NodeLibraryPanel` | 单击选中、双击添加、快速连续双击、拖拽出库 |
| `VariableEditorPanel` | 表格编辑、Enter 提交、拖动行/调整列宽与编辑并发 |
| `AutomationWidget` | 运行/停止/暂停/恢复按钮与上述编辑操作的任意交错 |

审计输出：缺陷清单表，每行含【编号｜控件｜根因模式(P1-P6)｜文件:行号｜触发序列｜风险等级】。SYM-1/2/3 必须出现在表中并定位到具体代码路径；若定位不到，用 QTest 复现实验反推（允许在本阶段写临时复现代码，但不修产品代码）。

**Phase 1 验收**：缺陷清单表提交入库（`reports/audit_automation.md`），SYM-1/2/3 有明确根因结论。

---

## 5. Phase 2 — QTest 复现用例（修复前必须红）

1. 为 Phase 1 清单中**每一项**编写 QTest 用例，放在 `motor_antomation/tests/unit/ui/`（目录不存在则创建），命名 `test_<控件名>_interaction.cpp`。
2. 用 `QTest::keyClicks` / `keyClick(Qt::Key_Return)` / `mousePress` / `mouseMove` / `mouseRelease` 精确复刻操作序列，SYM-1 必须包含「键入 → Enter → 立即 mousePress+mouseMove」的时序。
3. 每个用例用 `add_test()` 注册进 `tests/CMakeLists.txt`（CLAUDE.md D3）。
4. 对"卡死"类缺陷：用例必须带**看门狗**（共享规格 §2 的事件循环延迟探针或 `QTest::qWait` + 总超时断言），卡死时用例判失败而不是挂起。`QTest` 用例自身超时用 ctest 的 `set_tests_properties(... PROPERTIES TIMEOUT 60)` 约束。
5. 运行确认：新增用例在修复前失败（红），失败信息与缺陷一一对应。

**Phase 2 验收**：`ctest -N` 列出全部新用例；每个用例修复前为失败/超时状态并有输出证据。

---

## 6. Phase 3 — 统一修复

1. 按根因模式分组修复：**同一 P 编号的问题必须用同一方案一次修完**（例如 P1 统一引入"参数行包生命周期单一所有者"方案，而不是每个按钮各补一个判空）。
2. 优先在公共层设防：能在控件基类 / 公共工具中加的防护（如重建表单前统一断连信号、统一的 `deleteLater` 规约、拖拽期间的更新节流），不要散落在各个槽函数里。
3. 对 P4 事件风暴：拖动/缩放/重绘必须节流（如合并 mouseMove、按帧率限制重绘），修复后需用 Phase 4 的压力测试证明。
4. 每修一组，立即跑 `gate.bat` + `ctest`，保证不引入回归（先绿再提交）。
5. 红线文件中发现的问题：写入报告「需集成代理处理」清单，**不改**。

**Phase 3 验收**：Phase 1 清单中全部条目的修复提交完成；每个修复提交附带对应回归测试由红转绿的证据。

---

## 7. Phase 4 — Debug 模拟仿真压力测试（本夜核心环节）

1. 新建压力测试可执行文件 `tests/stress/stress_automation_ui.cpp`（`add_test()` 注册，T TIMEOUT 设为 600）：
   - 构造 `AutomationWidget` + `AutomationEngine` + 一个示例 FlowGraph（≥10 节点含 If/Assert/参数节点）。
   - **随机交互模糊测试**：用固定种子的伪随机序列，从以下操作池随机抽取并连续执行 ≥500 次：键入随机文本、按 Enter、Tab、双击节点库、选中节点、编辑各类型参数、删除/添加参数行、拖动节点、拖拽连线、滚轮缩放、运行/停止/暂停按钮。每步之间 0~5ms 随机延迟。
   - 全程挂载事件循环延迟看门狗（共享规格 §2）：任何一次事件处理 >300ms 即 `QFAIL` 并输出当时的操作序列。
   - 结束断言：无崩溃、无看门狗触发、控件树状态自检通过（节点数/参数行无泄漏式增长）。
2. 压力测试必须跑 **3 轮不同种子**全绿才算通过。
3. 若压力测试暴露新问题 → 回到 Phase 1 补清单 → 重走 Phase 2/3 → 再跑 Phase 4。**这是允许且预期的循环**。

**Phase 4 验收**：3 轮压力测试全绿，看门狗零触发，报告附 ctest 输出。

---

## 8. 通宵循环协议（agent 自主运转规则）

1. 循环体：发现问题 → 定位 → 写回归用例（红）→ 修复 → 全量 gate+ctest（绿）→ 提交 → 继续下一项。
2. 每完成一轮循环，追加一段到 `reports/nightly_report_automation.md`：时间、修了什么、根因模式、测试证据、剩余清单。
3. 遇到无法修复的问题：不得硬改、不得删除测试绕过；记入报告「阻塞项」，说明原因与所需支持（例如需要集成代理动红线文件），继续处理下一项。
4. 遇到卡死无法定位：用 offscreen 最小化复现 + 在可疑路径加 `qDebug` 计时输出锁定阻塞段，**禁止靠加 sleep 掩盖**。
5. 收尾（或清单清空时）：按 §11 验收矩阵逐项打勾，写最终报告，提交并 push 本分支（`git push origin domain/automation`）。

---

## 9. 完成定义（本任务专属，叠加 CLAUDE.md §10 的 D1-D8）

| # | 检查项 | 判定方法 |
|---|---|---|
| N1 | Phase 1 缺陷清单全部闭环（修复或标注需集成代理处理） | `reports/audit_automation.md` 状态列 |
| N2 | 每条缺陷有回归测试且全绿 | `ctest --output-on-failure` |
| N3 | 压力测试 3 种子 ×500 次操作零卡死零崩溃 | stress 测试 ctest 输出 |
| N4 | 事件循环看门狗全程零触发（阈值 300ms） | 压力测试日志 |
| N5 | SYM-1/2/3 三个用户症状有专门回归用例且通过 | `ctest -N` 命名核对 |
| N6 | 最终报告入库并 push | `reports/nightly_report_automation.md` + git log |

---

## 10. 禁止事项（违反任何一条即任务失败）

1. 禁止跳过审计直接改代码；禁止"改一处试一处"的打补丁模式。
2. 禁止修改 master-owned 红线文件（mainwindow.*、src/core/**、src/databus/**、src/logging/**、src/app/**、DynamicWidgetFactory、WidgetBindingManager）。
3. 禁止删除、跳过、弱化既有测试来换取绿灯。
4. 禁止用 `sleep`/`qWait` 掩盖时序问题。
5. 禁止把本分支改动直接合入 master（合并由集成代理在主 checkout 执行）。
6. 禁止提交构建产物（build*/）。

---

## 11. 验收矩阵（收尾时逐项打勾并留证据）

| 编号 | 验收项 | 证据位置 |
|---|---|---|
| A1 | Phase 0 基线报告 | reports/nightly_baseline_automation.md |
| A2 | 缺陷审计清单（含 SYM-1/2/3 根因） | reports/audit_automation.md |
| A3 | 回归用例齐备且修复前红/修复后绿 | ctest 输出 + git 提交对照 |
| A4 | 统一修复完成，同根因同方案 | git log（[自动化] 提交） |
| A5 | 压力测试 3 种子全绿 | ctest 输出 |
| A6 | gate.bat 全绿（编译+测试+静态） | gate 日志 |
| A7 | 最终报告 + push 完成 | reports/nightly_report_automation.md、git ls-remote |

---

*文档结束。执行 agent 开工前先通读本文件与共享规格；天亮时用户以 §11 矩阵和最终报告验收。*
