# Motor Automation Studio — 用户使用说明书

> **版本**: v1.4 | **日期**: 2026-08-07

---

## 1. 软件简介

Motor Automation Studio 是一款工业级电机调试与自动化测试平台，支持串口通信、实时波形显示、仪表盘监控、参数管理、故障检测、自动化测试等功能。

### 1.1 主界面布局

```
┌──────────┬──────────────────────────┬──────────┐
│  导航     │       工作区              │  属性面板  │
│          │                          │          │
│ 仪表盘    │                          │          │
│ 波形      │                          │          │
│ 自动化    │                          │          │
│ 设备      │                          │          │
│ 设置      │                          │          │
├──────────┴──────────────────────────┴──────────┤
│                 日志控制台                       │
└────────────────────────────────────────────────┘
```

- **左侧导航栏**: 切换功能页面
- **中间工作区**: 显示当前页面内容
- **右侧属性面板**: 显示当前页面的属性信息
- **底部日志控制台**: 显示运行日志

---

## 2. 功能页面

### 2.1 仪表盘（Dashboard）

仪表盘以卡片形式显示电机运行的关键指标：

| 卡片 | 说明 |
|------|------|
| 电机状态 | 已停止 / 运行中 / 故障 |
| 通信状态 | 已断开 / 已连接 / 超时 |
| 电压 | 实时电压值 |
| 电流 | 实时电流值 |
| 转速 | 电机转速 (RPM) |
| 温度 | 电机温度 (°C) |
| 故障计数 | 当前活动故障数量 |

**告警级别**: 绿色(正常) → 黄色(警告) → 红色(严重)

### 2.2 波形 / 曲线显示

实时显示电机数据的波形曲线。采用单列多行滚动子图布局，每个子图独立配置。

**子图管理**:
| 操作 | 方式 |
|------|------|
| 新增子图 | 点击 "+ 新增子图" 按钮 |
| 删除子图 | 点击子图标题栏 × 按钮 |
| 调整子图高度 | 拖拽子图底部分隔线 |
| 重命名子图 | 双击子图标题栏名称 |
| 配置通道 | 子图内通道栏右键选择通道 |
| 滚动整个区域 | 鼠标左键拖拽（滚轮不滚动区域，只缩放曲线） |

**曲线操作**:
| 操作 | 方式 |
|------|------|
| 时间轴缩放 | 鼠标滚轮（鼠标在哪个子图上就缩放哪个图） |
| 值轴缩放 | Ctrl + 鼠标滚轮 |
| 框选放大 | 先点击工具栏"框选"按钮，再左键拖拽矩形区域 |
| 自适应 | 双击曲线区域 |
| 平移 | 鼠标右键拖拽 |
| 滚动整个区域 | 鼠标左键拖拽（默认状态，不开启框选时） |

**时间轴联动**:
- 默认所有子图共用时间轴（任一子图缩放/平移，其他同步联动）
- 每个子图可切换为独立时间轴（子图标题栏 "链接/独立" 按钮）

**曲线管理器**（曲线窗口下方）:
- 查看所有通道的名称、颜色、单位、Y轴范围
- 双击颜色可修改（全局生效）
- 双击名称/单位可编辑
- 勾选/取消"可见"控制曲线显示/隐藏
- 删除按钮可移除通道

### 2.3 自动化测试（流程图编辑器）

用于创建和执行自动化测试流程。支持两种视图：**表格视图**和**流程图视图**（默认），通过工具栏按钮切换。

#### 流程图视图

**布局**：
- 左侧：节点库面板（按分类列出所有节点类型，双击添加）
- 中央：FlowCanvas 画布（拖拽节点、连线、框选）
- 右侧：参数面板（选中节点后编辑参数）

**节点类型（22种）**：

| 分类 | 节点 |
|------|------|
| 控制 | 设置参数, 启动电机, 停止电机, 速度斜坡, 自定义命令 |
| 时序 | 延时, 等待条件, 定时器 |
| 逻辑 | 判断 if, 循环 loop, 分支 switch, 跳转 |
| 数学 | 计算, 变量赋值 |
| 通信 | 读参数, 写寄存器 |
| 数据 | 记录数据, 日志输出, 导出 |
| 断言 | 断言 |
| 异常 | 异常处理 |
| 流程 | 子流程, 注释 |

**画布操作**:
| 操作 | 方式 |
|------|------|
| 添加节点 | 双击节点库中的节点或拖入画布 |
| 移动节点 | 鼠标拖拽 |
| 创建连线 | 从节点输出端口拖到另一节点（松开在节点主体或输入端口即可） |
| 删除节点/连线 | 选中后按 Delete 键 |
| 编辑参数 | 选中节点后右侧面板编辑 |
| 画布缩放 | Ctrl+滚轮 |
| 右键菜单 | 编辑参数 / 删除节点 |

**表达式系统**：
- 支持四则运算、比较、逻辑（&& / || / !）、函数（min/max/abs/avg）
- 变量引用：`$变量名`
- 通道引用：`channel:通道名`
- 例：`(channel:Ia + channel:Ib + channel:Ic) / 3 > 10`
- 例：`$温度 < 85 && $转速 > 1400`

**节点执行支持**：
- 已实现执行：设置参数、启动/停止电机、延时、判断 if、循环、计算、变量赋值、读参数、记录数据、日志输出、断言、子流程、注释
- 尚未实现执行（运行时会明确报"未实现的节点类型"）：速度斜坡、自定义命令、定时器、分支 switch、跳转、写寄存器、发送命令、导出、异常处理
- 旧表格 JSON 自动转换流程图：SetParameter 多参数自动拆分为多个节点，Assert 的 channel/min/max 自动转换为条件表达式

**运行控制**：
1. 点击 **Load Test...** 加载 JSON 流程图文件
2. 点击 **Run** 开始执行（当前执行节点高亮为金色）
3. **Pause** 暂停 / **Resume** 继续 / **Stop** 停止（均可正常响应）
4. 执行完成后显示汇总结果

**兼容性**：支持加载旧版表格型 JSON 测试用例，自动转换为流程图。

#### 表格视图

保留原有表格步骤编辑和执行功能，适合简单顺序测试。

**步骤状态**:
- ▶ 蓝色 = 正在执行
- ✓ 绿色 = 通过
- ✗ 红色 = 失败
- — 灰色 = 跳过

**JSON 用例格式**:
```json
{
    "name": "HighSpeedTest",
    "description": "高速电机测试",
    "steps": [
        {
            "type": "StartMotor",
            "description": "启动电机",
            "timeoutMs": 5000
        },
        {
            "type": "SetParameter",
            "description": "设置转速",
            "params": [{"Speed": "3000"}],
            "timeoutMs": 1000
        },
        {
            "type": "Wait",
            "description": "等待稳定",
            "timeoutMs": 3000
        },
        {
            "type": "Assert",
            "description": "检查电流",
            "params": [{"Ia": "min=0.1", "Ia": "max=10.0"}],
            "timeoutMs": 1000
        },
        {
            "type": "StopMotor",
            "description": "停止电机",
            "timeoutMs": 5000
        }
    ]
}
```

**支持的步骤类型**:
| 类型 | 说明 |
|------|------|
| StartMotor | 启动电机 |
| StopMotor | 停止电机 |
| SetParameter | 设置参数 |
| ReadParameter | 读取参数 |
| Wait | 等待指定毫秒 |
| Assert | 断言检查（min/max/equals） |
| RecordData | 记录数据 |
| Custom | 自定义步骤 |

**测试报告**: 执行完成后自动生成 HTML 和 CSV 报告，保存在 `./reports/` 目录。

### 2.4 Device（设备）

显示设备连接信息和状态。

### 2.5 Settings（设置）

参数管理页面，可查看和编辑参数配置。

---

## 3. 工具栏操作

| 按钮/控件 | 功能 |
|-----------|------|
| 端口下拉框 | 选择串口 |
| 波特率下拉框 | 选择通信波特率 |
| Connect | 连接设备 |
| Disconnect | 断开连接 |
| Refresh | 刷新串口列表 |
| **+** | 创建动态控件 |

---

## 4. 动态控件

点击工具栏 **+** 按钮可创建自定义控件：

| 类型 | 说明 |
|------|------|
| Button | 点击发送绑定的命令 |
| Slider | 拖动滑块发送命令（{value} 替换为当前值） |
| Input | 输入文本后回车发送命令 |

**右键菜单**: 编辑绑定 / 删除控件

---

## 5. 菜单功能

| 菜单 | 功能 |
|------|------|
| File → Import Project | 导入工程文件 (JSON) |
| File → Export Project | 导出工程文件 (JSON) |
| File → Export CSV | 导出数据为 CSV |
| File → Save Project | 保存当前工程 |
| File → Load Project | 加载工程文件 |
| File → Exit | 退出程序 |
| 设置(S) → 通道配置 | 通道配置（名称/单位/颜色/缩放） |
| Settings → Settings | 系统设置 |
| Help → About | 关于 |

---

## 6. 通道配置

通过 **Settings → Channel Configuration** 打开通道配置对话框：

| 列 | 说明 |
|----|------|
| Name | 通道名称 |
| Unit | 单位 (如 A, RPM, °C) |
| Type | 数据类型 (float/int32/uint16) |
| Scale | 缩放系数 |
| Offset | 偏移量 |
| Color | 双击修改颜色 |

点击 **Apply** 保存修改，曲线和仪表盘会自动更新。

---

## 7. 快捷键

| 快捷键 | 功能 |
|--------|------|
| 鼠标滚轮 | 曲线时间轴缩放（不滚动整个区域） |
| Ctrl + 鼠标滚轮 | 曲线值轴缩放 |
| 左键拖拽 | 滚动整个区域（默认）/ 框选放大（开启"框选"后） |
| 右键拖拽 | 曲线平移 |
| Ctrl+S | 保存工程 |
| Ctrl+O | 打开工程 |

---

## 8. 通信协议

支持 **VOFA+** 协议的两种数据格式：

- **JustFloat**: 原始 float 字节流（每个通道 4 字节小端序）
- **FireWater**: 带帧尾的 ASCII 浮点格式

设备需配置为输出 VOFA+ 兼容格式数据。

---

## 9. 文件格式

| 文件类型 | 扩展名 | 说明 |
|----------|--------|------|
| 工程文件 | .json | 软件配置和参数 |
| 测试用例 | .json | 自动化测试脚本 |
| 数据导出 | .csv | 波形数据导出 |
| 测试报告 | .html / .csv | 自动化测试结果 |
