# 通讯域（domain/comms）夜间防卡死自测任务书

> 文档版本：v1.0 ｜ 编制日期：2026-08-09 ｜ 编制角色：集成代理代拟，执行者 = 通讯域执行 agent
> 执行方式：**通宵全自动**。本文件交给一个独立 agent 线程，在 `E:\My_project\QT\Motor_Antomation-wt\comms` 内循环执行「审计 → 复现 → 修复 → 压力测试 → 门禁验证」，**直到 §11 验收矩阵全绿为止**。除 C 盘文件操作外不需要人工介入。
> 共享规格：通用机制（卡死判定、看门狗、报告格式、合并规则）见 master 分支 `docs/夜间防卡死自测_共享执行规格.md`（本 worktree 尚无时，先执行 Phase 0 的 `git merge master` 拉入）。

---

## 0. 任务目标与零容忍标准

1. **零卡死**：通讯域全部界面与数据链路，在任何用户交互与任何设备/数据状态下不得出现界面冻结 / 无响应 / 假死（判定：UI 线程事件循环单次阻塞 > 300ms，见共享规格 §2）。
2. **零崩溃**：不得出现段错误、堆损坏、未捕获异常、跨线程误用 Qt 对象导致的崩溃。
3. **禁止打补丁式修复**：先建全局缺陷清单，**同一类根因用同一方案一次修完**，每类根因配回归测试。
4. 覆盖范围（CLAUDE.md §2）：`src/communication/**`、`src/device/**`、`src/data/**`、`src/ui/DashboardWidget`、`FaultWidget`。

---

## 1. 本域特有高危场景（审计重点）

### 1.1 用户当前反馈的通用症状（全域共性，本域同样必查）

| 编号 | 操作序列 | 现象 |
|---|---|---|
| SYM-1 | 在任意输入控件中输入数据 → **立刻按 Enter** → **100ms 内拖动控件** | 界面卡死 |
| SYM-2 | 输入后不按 Enter 直接拖拽 / 切换焦点 | 偶发卡死 |

### 1.2 通讯域专属高危模式

| 模式 | 说明 | 高发位置 |
|---|---|---|
| C1 跨线程 Qt 对象误用 | `DeviceWorker` 在工作线程产生数据/信号，UI 直接连接或工作线程里操作 QWidget | `DeviceWorker` ↔ `DashboardWidget`/`FaultWidget` |
| C2 高频刷新风暴 | 数据帧率高时每帧刷新全部 Dashboard 卡片（重绘/文本布局风暴） | `DashboardWidget`（卡片刷新路径） |
| C3 连接状态机竞争 | 连接/断开/超时 状态切换与 UI 操作交错；重复点连接按钮；断开时后台仍在解析 | `DeviceManager`、`SerialTransport`/`TcpTransport`/`LoopbackTransport` |
| C4 异常帧 / 畸形数据 | 协议解析器遇到半帧、超长帧、非法浮点文本时阻塞或死循环 | `VofaParser`、`MotorProtocol`、`FrameCodec` |
| C5 命令队列堆积 | 发送命令队列无上限 / 阻塞等待应答导致 UI 线程等待 | `CommandQueue`、`DeviceWorker` |
| C6 录制与回放 | `DataRecorder` 录制中 UI 操作；`DataManager` 大数据量加载阻塞 UI | `src/data/**` |
| C7 故障列表风暴 | 故障事件高频到达时 FaultWidget 列表全量重建 | `FaultWidget` |

---

## 2. 执行规则（强制）

1. **四阶段顺序不可跳过**：Phase 1 审计 → Phase 2 复现用例 → Phase 3 统一修复 → Phase 4 仿真压力测试。每 Phase 结束输出阶段报告再继续。
2. **只改本域名下文件**（§0.4）。根因落在 master-owned 红线文件（`mainwindow.*`、`src/core/**`、`src/databus/**`、`src/logging/**`、`src/app/**`）时**不得修改**，报告中标注「需集成代理处理」。注意：`src/databus/**` 属 master，本域只能消费不能改。
3. **证据优先**：一切"已修复/已验证"必须附命令输出（CLAUDE.md §10）。
4. **每处修复配套回归测试**：修复前红、修复后绿，否则视为无效。
5. 提交规范：前缀 `[通讯]`，写清根因模式编号（C1-C7 / P1-P6，P 编号见共享规格 §3）。

---

## 3. Phase 0 — 环境准备与基线

1. `cd E:\My_project\QT\Motor_Antomation-wt\comms`，`git status`；本任务之外的未提交改动先按归属提交或 stash。
2. **同步 master**：`git merge master`（master 已有门禁基础设施与测试修复提交，域分支落后必须先合并）。冲突只在 master-owned 文件 → 上报，不自行改写。
3. 构建 + 全量测试建立基线：
   ```
   scripts\gate.bat     （本 worktree 无此文件时用主 checkout：E:\My_project\QT\Motor_Antomation\scripts\gate.bat，工作目录设为本 worktree 的 motor_antomation\）
   ctest --test-dir build\ci-tests --output-on-failure
   ```
4. 基线写入 `reports/nightly_baseline_comms.md`。
5. UI 测试一律 `QT_QPA_PLATFORM=offscreen`；**无真实串口**：一律用 `DeviceSimulator` + `LoopbackTransport` 模拟设备与链路（参考 `tests/integration/` 既有管线测试）。

**Phase 0 验收**：基线报告产出，gate.bat 结果明确。

---

## 4. Phase 1 — 只读审计（不改代码）

审计矩阵：

| 对象 | 必须审计的场景 |
|---|---|
| `DashboardWidget` | 1kHz 级数据流下卡片刷新、阈值告警状态切换、窗口 resize、卡片增删、SYM-1/2 交互序列 |
| `FaultWidget` | 故障事件风暴（短时间大量故障）、故障确认/清除与事件到达交错、列表滚动 |
| `DeviceManager` / `DeviceWorker` | 连接→断开→重连快速循环、断开时正在传输、重复连接请求、线程退出与重建 |
| `VofaParser` / `MotorProtocol` / `FrameCodec` | 半帧、粘包、超长帧、非法数值文本、字节流随机切分喂入（fuzz 思路） |
| `CommandQueue` | 队列打满、应答超时、乱序应答、队列清理与发送交错 |
| `DataRecorder` / `DataManager` | 录制中连接断开、录制中 UI 操作、大文件加载 |
| 传输层各实现 | 构造/析构与数据到达交错（重点 LoopbackTransport，其余看代码路径） |

审计输出：缺陷清单表【编号｜对象｜根因模式(C1-C7/P1-P6)｜文件:行号｜触发序列｜风险等级】，SYM-1/2 在本域的实例必须定位到具体代码路径。

**Phase 1 验收**：清单入库 `reports/audit_comms.md`。

---

## 5. Phase 2 — QTest 复现用例（修复前必须红）

1. 为清单每项写 QTest 用例：`motor_antomation/tests/unit/ui/test_<控件名>_interaction.cpp`（界面类）与 `tests/unit/communication/test_<组件>_robustness.cpp`（链路类）。目录不存在则创建。
2. 交互用 `QTest::keyClicks / mousePress / mouseMove` 精确复刻，SYM-1 必须含「键入 → Enter → 立即拖动」时序；链路类用例用 `DeviceSimulator`/`LoopbackTransport` 构造数据流，协议解析用例用随机切分喂入。
3. 全部 `add_test()` 注册（D3）；卡死类用例带看门狗（共享规格 §2），`TIMEOUT 60` 防挂起。
4. 确认修复前全部失败/超时（红）。

**Phase 2 验收**：`ctest -N` 列全新用例，修复前红有证据。

---

## 6. Phase 3 — 统一修复

1. 同根因模式一次修完（如 C1 统一建立"跨线程一律 queued connection + 线程归属断言"规约；C2 统一做卡片按帧合并刷新）。
2. 优先公共层设防：传输层生命周期规约、解析器健壮性（永不阻塞、永不死循环、对任意字节流可恢复）、队列上限与丢弃策略，写进域内公共约定并落实。
3. C2/C7 类刷新风暴修复必须用 Phase 4 高频压力证明。
4. 每修一组立即 `gate.bat` + `ctest`，先绿再提交。
5. 红线文件问题只记录、不修改。

**Phase 3 验收**：清单全部闭环或标注移交；每个修复提交附测试由红转绿证据。

---

## 7. Phase 4 — Debug 模拟仿真压力测试（本夜核心环节）

1. 新建 `tests/stress/stress_comms_ui.cpp`（`add_test()`，TIMEOUT 600）：
   - 构造 `DashboardWidget` + `FaultWidget` + `DeviceManager` + `DeviceSimulator`（经 `LoopbackTransport`），模拟设备持续高频上报。
   - **链路压力 + 随机交互叠加**：模拟链路以 ≥1kHz 帧率供数并周期性注入异常帧/断连重连；同时固定种子伪随机序列连续执行 ≥500 次操作：连接/断开按钮、重复点连接、Dashboard 输入控件键入+Enter+拖动（SYM-1）、故障确认/清除、窗口 resize、录制开关切换。每步间 0~5ms 随机延迟。
   - 全程挂事件循环延迟看门狗（共享规格 §2）：单次事件处理 >300ms 即 `QFAIL`，输出当时操作序列与链路状态。
   - 结束断言：无崩溃、看门狗零触发、断连重连 100 轮全部成功、无对象泄漏（卡片/列表项计数自检）。
2. **3 轮不同种子**全绿才算通过。
3. 压力测试暴露新问题 → 回 Phase 1 补清单 → 重走 2/3 → 再跑 4。允许且预期循环。

**Phase 4 验收**：3 轮全绿、看门狗零触发、ctest 输出入报告。

---

## 8. 通宵循环协议（agent 自主运转规则）

1. 循环体：发现问题 → 定位 → 回归用例（红）→ 修复 → gate+ctest（绿）→ 提交 → 下一项。
2. 每轮追加 `reports/nightly_report_comms.md`：时间、修复内容、根因模式、测试证据、剩余清单。
3. 无法修复：不硬改、不删测试绕过；记「阻塞项」继续下一项。
4. 卡死难定位：offscreen 最小化复现 + `qDebug` 计时锁定阻塞段；**禁止加 sleep 掩盖**。
5. 收尾：§11 逐项打勾 → 最终报告 → 提交并 push（`git push origin domain/comms`）。

---

## 9. 完成定义（叠加 CLAUDE.md §10 D1-D8）

| # | 检查项 | 判定方法 |
|---|---|---|
| N1 | Phase 1 清单全部闭环（修复或标注移交） | `reports/audit_comms.md` |
| N2 | 每条缺陷有回归测试且全绿 | `ctest --output-on-failure` |
| N3 | 链路+UI 压力测试 3 种子 ×500 次操作零卡死零崩溃 | stress ctest 输出 |
| N4 | 看门狗全程零触发（300ms） | 压力测试日志 |
| N5 | SYM-1/2 有专门回归用例且通过 | `ctest -N` |
| N6 | 最终报告入库并 push | `reports/nightly_report_comms.md` + git log |

---

## 10. 禁止事项

1. 禁止跳过审计直接改代码；禁止打补丁模式。
2. 禁止修改 master-owned 红线文件（含 `src/databus/**`）。
3. 禁止删除/跳过/弱化既有测试换绿灯。
4. 禁止用 `sleep`/`qWait` 掩盖时序问题。
5. 禁止本分支直接合入 master（由集成代理在主 checkout 执行）。
6. 禁止提交构建产物。

---

## 11. 验收矩阵

| 编号 | 验收项 | 证据位置 |
|---|---|---|
| A1 | Phase 0 基线报告 | reports/nightly_baseline_comms.md |
| A2 | 缺陷审计清单 | reports/audit_comms.md |
| A3 | 回归用例修复前红/修复后绿 | ctest 输出 + git 提交 |
| A4 | 统一修复完成 | git log（[通讯] 提交） |
| A5 | 压力测试 3 种子全绿 | ctest 输出 |
| A6 | gate.bat 全绿 | gate 日志 |
| A7 | 最终报告 + push | reports/nightly_report_comms.md、git ls-remote |

---

*文档结束。执行 agent 开工前先通读本文件与共享规格；天亮时以 §11 矩阵和最终报告验收。*
