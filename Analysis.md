# Benjamin ESC Tool（VESC Tool / BLDC Tool）深度分析

> 分析日期：2026-08-04
> 分析对象：Benjamin Vedder 的 VESC Tool（原名 BLDC Tool）
> 源码仓库：https://github.com/vedderb/vesc_tool / https://github.com/vedderb/bldc-tool
> 目标：提取设计思想，用于指导自研电机调试上位机

---

## 一、总体架构设计思想

### 1.1 核心哲学：上位机是"下位机的透明窗口"

VESC Tool 的设计核心思想是：**上位机不做任何控制决策，只是将下位机（MCU）的所有状态和执行能力以可视化方式呈现给用户**。所有控制逻辑（电流环、速度环、保护策略）都在下位机 MCU 上运行，上位机只负责：

- **配置下发**（Write Configuration）
- **状态读取**（Read Configuration / Get Values）
- **实时数据可视化**（Real-time Data）
- **固件升级**（Firmware Upload）

### 1.2 架构分层

```
┌─────────────────────────────────────────────────────┐
│                   GUI Layer (Qt)                      │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌─────────┐ │
│  │ 连接管理  │ │ 参数配置页│ │ 实时数据页│ │ 终端页   │ │
│  │(Connect) │ │(Tabs)    │ │(RT Data) │ │(Terminal)│ │
│  └──────────┘ └──────────┘ └──────────┘ └─────────┘ │
├─────────────────────────────────────────────────────┤
│               Packet Interface Layer                  │
│  ┌──────────────────────────────────────────────────┐│
│  │  CRC16 校验 + 状态机解包 + 超时重试 + 固件升级   ││
│  └──────────────────────────────────────────────────┘│
├─────────────────────────────────────────────────────┤
│               Transport Layer                        │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐            │
│  │ USB CDC  │ │  CAN Bus │ │ UDP(WiFi)│            │
│  │(ttyACMx) │ │          │ │          │            │
│  └──────────┘ └──────────┘ └──────────┘            │
└─────────────────────────────────────────────────────┘
         │            │            │
         ▼            ▼            ▼
    ┌─────────────────────────────────────┐
    │          MCU (STM32F4)              │
    │  ChibiOS RTOS + FOC 算法 + 保护逻辑 │
    └─────────────────────────────────────┘
```

---

## 二、通信协议设计（核心亮点）

### 2.1 二进制包协议

VESC 使用自定义二进制包协议，而非文本协议（如 AT 指令、JSON），这是其高性能的根基：

```
┌──────┬──────┬──────────┬───────────┬──────┬──────┐
│ 0x02 │ 0x03 │ length   │ payload   │ CRC  │ 0x03 │
│(SOF1)│(SOF2)│ (2 bytes)│ (N bytes) │(2B)  │(EOF) │
└──────┴──────┴──────────┴───────────┴──────┴──────┘
```

**设计要点：**
- 双字节起始标志（0x02 0x03）降低误识别率
- 长度字段支持可变长度 payload
- CRC16 保证数据完整性
- 状态机解包：`START → LEN → DATA → CRC → END`
- 单字节结束标志（0x03）作为冗余校验

### 2.2 命令集设计

命令 ID 枚举（`COMM_PACKET_ID`）覆盖了所有操作：

| 类别 | 命令 | 说明 |
|------|------|------|
| 版本查询 | `COMM_FW_VERSION` | 获取固件版本（用于兼容性检查） |
| 固件升级 | `COMM_JUMP_TO_BOOTLOADER` | 跳转至 Bootloader |
| | `COMM_ERASE_NEW_APP` | 擦除 Flash |
| | `COMM_WRITE_NEW_APP_DATA` | 分块写入新固件 |
| 实时数据 | `COMM_GET_VALUES` | 获取电压/电流/转速/温度等 |
| 电机控制 | `COMM_SET_DUTY` | 占空比控制 |
| | `COMM_SET_CURRENT` | 电流控制 |
| | `COMM_SET_RPM` | 转速控制 |
| 配置管理 | `COMM_GET_MCCONF` | 读取电机配置 |
| | `COMM_SET_MCCONF` | 写入电机配置 |
| | `COMM_GET_MCCONF_DEFAULT` | 获取默认配置 |
| | `COMM_GET_APPCONF` | 读取应用配置 |
| | `COMM_SET_APPCONF` | 写入应用配置 |
| 参数检测 | `COMM_DETECT_MOTOR_PARAM` | 自动检测电机参数 |
| | `COMM_DETECT_MOTOR_R_L` | 测量电阻/电感 |
| | `COMM_DETECT_MOTOR_FLUX_LINKAGE` | 测量磁链 |
| | `COMM_DETECT_ENCODER` | 编码器检测 |
| | `COMM_DETECT_HALL_FOC` | Hall 传感器 FOC 检测 |
| 系统控制 | `COMM_REBOOT` | 重启 |
| | `COMM_ALIVE` | 心跳 |
| 调试 | `COMM_TERMINAL_CMD` | 终端命令 |
| | `COMM_PRINT` | 调试打印 |
| | `COMM_SAMPLE_PRINT` | 采样数据打印 |

**设计思想：每一项操作都有独立的命令 ID，而非通过参数区分。这使得协议解析简单、可靠。**

### 2.3 实时数据包（MC_VALUES）

```cpp
typedef struct {
    double v_in;              // 输入电压
    double temp_mos1..6;      // 6路 MOSFET 温度
    double temp_pcb;          // PCB 温度
    double current_motor;     // 电机电流
    double current_in;        // 输入电流
    double rpm;               // 转速（ERPM）
    double duty_now;          // 当前占空比
    double amp_hours;         // 累计安时（消耗）
    double amp_hours_charged; // 累计安时（回充）
    double watt_hours;        // 累计瓦时（消耗）
    double watt_hours_charged;// 累计瓦时（回充）
    int tachometer;           // 里程计（有符号）
    int tachometer_abs;       // 里程计（绝对值）
    mc_fault_code fault_code; // 故障码
} MC_VALUES;
```

**关键设计：用一个结构体打包所有实时数据，一次请求（`COMM_GET_VALUES`）一次回复，减少通信往返。**

---

## 三、配置参数布局设计

### 3.1 参数分类原则

VESC Tool 将参数分为两大类：

| 大类 | 包含内容 | 说明 |
|------|----------|------|
| **Motor Configuration** | 电机参数、限值、FOC 参数、PID 参数 | 与电机/硬件强相关 |
| **App Configuration** | 控制模式、输入源配置、超时设置 | 与应用场景相关 |

### 3.2 Motor Configuration 的子页面布局

```
Motor Configuration
├── General     — 电机类型、传感器模式、换向模式
├── Limits      — 电流/电压/温度/RPM 限制
├── Sensorless  — 无感参数（BEMF 耦合、积分器限值）
├── FOC         — FOC 参数（电流环PI、观测器增益、PLL）
├── Additional  — 相位超前、启动参数
├── Gearing     — 减速比、轮径等机械参数
└── Advanced    — 高级调优参数
```

**设计思想：**
- **从通用到专用**：General → Limits 是每个用户必配的，FOC/Advanced 是专家才需要接触的
- **相关参数聚集**：电流相关的所有限制都在 Limits 页面，传感器参数都在 Sensorless 页面
- **默认值驱动**：每个参数都有合理的默认值，用户只需改动关键参数

### 3.3 App Configuration 的子页面布局

```
App Configuration
├── General     — 应用类型选择、控制器ID、超时设置
├── PPM         — RC 遥控器配置
├── ADC         — 模拟量控制配置
├── UART        — 串口控制配置
├── Nunchuk     — Wii Nunchuk 控制
└── NRF         — 无线 NRF 配置
```

**设计思想：每种输入源独立一个页面，互不干扰。选择哪种应用就用哪种配置。**

### 3.4 参数值的"软策略"

VESC 在参数限制上使用了"软退避"策略而非硬切断：

- **电流限制**：达到限制后不是立即切断，而是逐步降低输出 → 电机保持运行
- **温度限制**：Start 温度开始降额，End 温度才完全切断 → 线性过渡
- **RPM 限制**：Start 开始降扭矩，End 才切断 → 平滑限速

**对 UI 设计的影响：** 每个限制参数都需要两个值（起始值 + 终止值），而非简单的单阈值。

---

## 四、实时波形与曲线刷新

### 4.1 绘图库选择

VESC Tool 使用 **QCustomPlot**（一个轻量级 Qt 绘图库），而非 QChart。原因：
- QCustomPlot 是单文件 `.h` + `.cpp`，零依赖，易于集成
- 性能优异，支持大量数据点的实时刷新
- 支持缩放、拖拽、导出图片等交互

### 4.2 实时数据刷新机制

```
┌──────────┐   定时器触发     ┌──────────────┐
│ QTimer   │ ──────────────→ │ timerSlot()   │
│ ~10-50Hz │                 │ 发送 GET_VALUES│
└──────────┘                 └──────┬───────┘
                                    │
                                    ▼
┌──────────┐   收到数据包     ┌──────────────┐
│ MCU      │ ──────────────→ │ valuesReceived│
│ 下位机   │   MC_VALUES     │  信号         │
└──────────┘                 └──────┬───────┘
                                    │
                                    ▼
                            ┌──────────────┐
                            │ 更新 Plot 曲线│
                            │ 更新数值显示  │
                            └──────────────┘
```

**关键设计：**
1. **定时轮询**：上位机以固定频率（如 10-50Hz）主动请求 `COMM_GET_VALUES`
2. **批量传输**：一次请求返回所有实时数据（电压、电流、转速、温度、故障码等），不是每个变量单独请求
3. **信号槽解耦**：`PacketInterface` 通过 Qt 信号 `valuesReceived(MC_VALUES)` 通知 UI 层，UI 层自行决定如何展示

### 4.3 曲线显示策略

从 VESC Tool 截图和源码分析：

- **多曲线叠加**：同一图表可同时显示多条曲线（如电流 + 转速 + 电压）
- **滚动窗口**：X 轴固定显示最近 N 秒的数据，旧数据自动滚动出视野
- **数值仪表盘**：曲线图旁边同时显示当前数值（如 12.3V、45.2A），便于精确读数
- **故障状态指示**：右下角状态栏显示连接状态、故障码

---

## 五、调试流程设计

### 5.1 标准调试流程

```
1. 连接设备 → 点击 "Connect" 按钮
         │
         ▼
2. 读取配置 → 点击 "Read Configuration"（从 MCU 读取当前配置）
         │
         ▼
3. 加载模板 → 点击 "Load XML"（从本地加载预设配置模板）
         │           （注意：先 Read 再 Load，因为 XML 可能不完整）
         ▼
4. 自动检测 → 点击 "Start Detection"（自动检测电机参数）
         │           （Integrator Limit, BEMF Coupling）
         ▼
5. 手动调参 → 修改电流/电压/温度/RPM 限制
         │
         ▼
6. 写入配置 → 点击 "Write Configuration"（将配置写入 MCU Flash）
         │
         ▼
7. 实时监控 → 切换到 RT Data 页面，观察波形
         │
         ▼
8. 导出/保存 → 点击 "Save XML"（保存配置到本地）
```

### 5.2 自动检测流程

VESC Tool 的电机参数自动检测是一个**带用户指导的半自动流程**：

```
用户点击 "Start Detection"
         │
         ▼
MCU 执行检测序列：
  1. 电机空载旋转加速
  2. 释放油门，让电机惯性滑行
  3. 低速运行测量 BEMF
         │
         ▼
MCU 返回检测结果：
  - cycle_int_limit（积分器限值）
  - bemf_coupling_k（BEMF 耦合系数）
         │
         ▼
用户手动填入参数框（或自动填入）
         │
         ▼
用户根据经验微调（如向下取整到 5 的倍数）
```

**设计思想：检测在 MCU 端执行（因为需要精确的时序控制），上位机只负责发起和显示结果。**

### 5.3 键盘控制电机

在调试阶段，可直接用键盘控制电机：

- **↑/↓ 方向键**：电流控制模式，正转/反转
- **←/→ 方向键**：占空比控制模式，正转/反转
- **右侧按钮**：鼠标点击控制

**设计思想：提供多种控制方式，方便在不同调试场景下快速测试。**

---

## 六、自动识别设备

### 6.1 连接机制

VESC Tool 使用 USB CDC（Communications Device Class）协议，设备会被识别为 `/dev/ttyACMx`（Linux）或 `COMx`（Windows）。

**关键设计：**
- 不依赖 VID/PID 匹配，而是通过**协议握手**识别设备
- 连接后首先发送 `COMM_FW_VERSION` 获取固件版本
- 根据版本号判断兼容性，提示用户是否需要升级固件
- 支持多种传输层：USB CDC、CAN Bus、UDP（WiFi）、BLE

### 6.2 多设备支持

- 每个 VESC 有独立的 `Controller ID`（0-255）
- 通过 CAN Bus 可连接多个 VESC，上位机通过 ID 区分
- 从 VESC 需要开启 `Send status over CAN`

---

## 七、参数同步机制

### 7.1 配置同步方向

```
┌──────────┐   Read Configuration   ┌──────────┐
│  上位机   │ ◄──────────────────── │  MCU     │
│  (GUI)   │                        │ (Flash)  │
│          │ ────────────────────►  │          │
└──────────┘   Write Configuration  └──────────┘
```

**核心原则：以 MCU 端 Flash 存储为唯一真实数据源（Single Source of Truth）。**

### 7.2 配置变更流程

```
用户修改参数 → 点击 "Write Configuration" → MCU 写入 Flash → MCU 重启应用
```

**关键设计：**
- 修改参数后不会自动写入 MCU，需要用户显式点击 "Write Configuration"
- 写入后可能需要重启（`COMM_REBOOT`）使新参数生效
- 提供 "Read Default Configuration" 获取出厂默认值
- 配置以 XML 文件格式保存到本地，方便备份和分享

### 7.3 固件升级与配置保护

```
1. 读取当前配置 → 保存为 XML
2. 上传新固件（分块写入 Flash）
3. 加载 XML 配置 → 写入配置
```

**设计思想：固件升级会擦除配置，所以在升级前提醒用户备份配置。**

---

## 八、在线修改（Live Tuning）

### 8.1 哪些参数可以实时修改

| 参数类型 | 在线修改 | 需要重启 | 说明 |
|----------|----------|----------|------|
| 电流限制 | ✅ | ❌ | 实时生效 |
| 电压限制 | ✅ | ❌ | 实时生效 |
| 电机类型 | ❌ | ✅ | 需要重启 |
| 传感器模式 | ❌ | ✅ | 需要重启 |
| PID 参数 | ✅ | ❌ | 实时生效 |
| 应用类型 | ❌ | ✅ | 需要重启 |
| 控制模式 | ✅ | ❌ | 实时生效 |

### 8.2 实时参数生效机制

在线修改参数通过 `COMM_SET_MCCONF` 直接写入 MCU 的 RAM 配置副本，MCU 的控制循环在下一个周期读取新参数。不需要写入 Flash 就能生效，但断电会丢失。

**设计思想：区分"在线生效"和"持久化存储"，用户可以先在线调试，满意后再写入 Flash。**

---

## 九、Flash 保存机制

### 9.1 存储架构

```
MCU Flash
├── Bootloader 区
├── Application 区（固件）
└── Configuration 区（参数）
    ├── mc_configuration（电机配置）
    └── app_configuration（应用配置）
```

### 9.2 写入保护

- 配置写入前进行参数合法性校验
- CRC 校验确保 Flash 数据完整性
- 写入失败时保留旧配置
- 提供默认配置恢复功能

---

## 十、固件升级流程

### 10.1 升级状态机

```
IDLE → ERASE → WRITE_CHUNK → VERIFY → DONE
         ↑          │
         └─ 重试 ───┘（最多 N 次）
```

### 10.2 升级步骤

1. **发送 `COMM_JUMP_TO_BOOTLOADER`**：MCU 跳转到 Bootloader
2. **发送 `COMM_ERASE_NEW_APP`**：擦除 Application 区域
3. **分块发送 `COMM_WRITE_NEW_APP_DATA`**：逐块写入新固件
4. **校验 CRC**：确保写入完整
5. **MCU 自动重启**：进入新固件

### 10.3 上位机侧实现

```cpp
// 固件上传状态机
int mFirmwareState;      // 当前状态
int mFirmwarePtr;        // 写入指针
int mFirmwareTimer;      // 超时计时器
int mFirmwareRetries;    // 重试次数
QString mFirmwareUploadStatus; // 状态文本
```

**设计思想：固件升级在上位机侧实现分块+重试逻辑，MCU 侧保持简单。上位机通过进度条反馈升级进度。**

---

## 十一、电流/电压显示设计

### 11.1 显示维度

VESC Tool 同时显示以下电气参数：

| 参数 | 说明 | 用途 |
|------|------|------|
| V_in | 输入电压 | 电池电压监控 |
| Current Motor | 电机相电流 | 扭矩控制反馈 |
| Current In | 输入电流 | 电池电流监控 |
| Ah Consumed | 累计消耗安时 | 电量统计 |
| Ah Charged | 累计回充安时 | 再生制动统计 |
| Wh Consumed | 累计消耗瓦时 | 能量统计 |
| Wh Charged | 累计回充瓦时 | 能量回收统计 |

**设计思想：区分"电机侧"和"电池侧"电流，因为两者在 PWM 变换后不相等。同时提供瞬时值和累计值。**

### 11.2 显示方式

- **实时数值**：以数字形式显示在仪表盘区域
- **曲线**：在 RT Data 页面以波形图显示
- **累计值**：Ah/Wh 累计值以数字形式显示
- **故障状态**：颜色编码（绿色正常，红色故障）

---

## 十二、多平台支持设计

VESC Tool 支持 **Linux / Windows / macOS / Android / iOS**，其多平台策略：

- **Qt 框架**：跨平台 GUI 基础
- **qmake 构建系统**：平台无关的构建配置
- **条件编译**：不同平台使用不同的配置文件（`build_lin`, `build_android`, `build_macos`）
- **模块化**：`bleuart.cpp`（BLE 支持）、`bleuartdummy.cpp`（无 BLE 平台的桩实现）

---

## 十三、设计思想总结 —— 可借鉴的核心原则

### 13.1 架构原则

| 原则 | 说明 |
|------|------|
| **MCU 为中心** | 所有控制逻辑在 MCU 执行，上位机是"透明窗口" |
| **二进制协议** | 高性能自定义二进制协议，非 JSON/文本 |
| **批量传输** | 一次请求返回所有数据，减少往返 |
| **信号槽解耦** | 通信层与 UI 层通过 Qt 信号槽解耦 |
| **状态机解包** | 协议解析使用状态机，健壮、可扩展 |

### 13.2 参数管理原则

| 原则 | 说明 |
|------|------|
| **分类分层** | Motor Config / App Config 分离，子页面按功能分组 |
| **默认值驱动** | 每个参数有合理默认值，降低使用门槛 |
| **在线/持久分离** | 修改参数可在线生效，也可写入 Flash 持久化 |
| **XML 备份** | 配置可导出/导入 XML，方便备份、分享、版本管理 |
| **单数据源** | MCU Flash 为唯一真实数据源 |

### 13.3 调试体验原则

| 原则 | 说明 |
|------|------|
| **半自动检测** | 自动检测 + 用户确认，而非全自动黑盒 |
| **多控制方式** | 键盘、鼠标、物理输入源均可控制电机 |
| **实时反馈** | 波形图 + 数值仪表盘 + 故障状态指示 |
| **引导式配置** | 从 Read → Load → Detect → Tune → Write 的清晰流程 |
| **固件兼容检查** | 连接时自动检测固件版本，提示升级 |

### 13.4 可靠性原则

| 原则 | 说明 |
|------|------|
| **CRC 校验** | 每个数据包都有 CRC16 校验 |
| **超时重试** | 通信超时自动重试 |
| **软退避策略** | 限制触发时逐步降额，而非硬切断 |
| **故障码机制** | 明确的故障码枚举，便于诊断 |
| **心跳机制** | `COMM_ALIVE` 保持连接检测 |

---

## 十四、对你自研 QT 上位机的建议

基于以上分析，建议你的 Motor Monitor 上位机采用以下架构：

```
Motor Monitor
├── 通信层
│   ├── 协议包封装/解析（CRC + 状态机）
│   ├── 串口/USB CDC 传输
│   └── 超时重试/心跳机制
├── 数据模型层
│   ├── 实时数据（类似 MC_VALUES）
│   ├── 参数配置（类似 mc_configuration）
│   └── 故障码枚举
├── UI 层
│   ├── 连接管理面板
│   ├── 参数配置页（Tab 分组）
│   ├── 实时波形页（QCustomPlot 或 Qt Charts）
│   ├── 仪表盘面板（数值显示）
│   ├── 终端/日志页
│   └── 固件升级页
└── 工具层
    ├── 配置导入/导出（JSON 或 XML）
    ├── 数据记录/回放
    └── 自动化测试脚本
```

**关键建议：**
1. 使用 QCustomPlot 做实时波形（性能好、轻量、免费）
2. 通信协议设计为二进制包格式，预留扩展空间
3. 参数分为"设备配置"和"应用配置"两大类
4. 提供"在线修改"和"写入 Flash"两种写入模式
5. 实时数据采用定时轮询（10-50Hz），一次请求返回所有数据
6. 实现自动检测功能（电机参数、编码器偏移等）
7. 支持键盘快捷键控制电机
8. 提供故障码显示和故障历史记录