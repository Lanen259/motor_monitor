# motor_antomation 软件总体设计文档

> **版本**: v1.4  
> **编制日期**: 2026-08-04（初版），2026-08-05（产品需求访谈完成），2026-08-06（v1.4: 界面风格从深色工业风切换为白+浅蓝现代风）  
> **项目**: 工业级电机调试与自动化测试平台 (Motor Automation)  
> **技术栈**: Qt 5.14.2 / C++17 / qmake / MinGW 32-bit
> **UI 风格**: 白色背景 (#F5F7FA) + 浅蓝主色调 (#2196F3)，现代简洁风格
> **默认字体**: Microsoft YaHei（微软雅黑），支持中文
> **目标平台**: Windows（主力）/ Linux（后续）  

## 开发工作流（必读）
所有开发任务默认遵循 superpowers 技能工作流：
1. 先用 brainstorming 澄清需求并出设计
2. 用 writing-plans 写实施计划
3. 用 test-driven-development 实现（先写测试）
4. 完成后用 requesting-code-review 审查
5. 用 finishing-a-development-branch 收尾合并
多模块/并行/长任务时，用 conductor 编排（读 .conductor/ledger.md）
所有的操作只局限当前的文件夹，可以在当前的文件夹做任何事。

## 项目规则（Rules）
- **中文界面**: 默认字体为 Microsoft YaHei，所有 UI 文本使用中文
- **说明书同步**: 每次界面或功能修改后，必须同步更新 `USER_MANUAL.md`
- **说明书原则**: 说明书无日志，仅反映当前软件的实际状态
- **曲线操作**: 滚轮=时间轴缩放，Ctrl+滚轮=值轴缩放，右键拖拽=平移
- **自动提交**: 每次需求修改完成后自动执行 git commit

---

## 目录

- [第0章：产品定义与需求规格](#第0章产品定义与需求规格)
- [第0章：产品定义与需求规格](#第0章产品定义与需求规格)
- [第1章：参考软件设计思想分析](#第1章参考软件设计思想分析)
- [第2章：整体目录结构](#第2章整体目录结构)
- [第3章：模块划分](#第3章模块划分)
- [第4章：通讯架构](#第4章通讯架构)
- [第5章：实时数据总线](#第5章实时数据总线)
- [第6章：波形系统](#第6章波形系统)
- [第7章：参数系统](#第7章参数系统)
- [第8章：自动化测试框架](#第8章自动化测试框架)
- [第9章：脚本系统](#第9章脚本系统)
- [第10章：插件系统](#第10章插件系统)
- [第11章：日志系统](#第11章日志系统)
- [第12章：线程模型](#第12章线程模型)
- [第13章：三年RoadMap](#第13章三年roadmap)
- [附录A：全局架构Review](#附录a全局架构review)

---

## 第0章：产品定义与需求规格

> **本章状态**: 🟢 需求访谈完成（六轮）  
> **最后更新**: 2026-08-05（全部六轮访谈完成）

### 0.1 产品定位

| 维度 | 定义 |
|------|------|
| **软件名称** | Motor Automation（电机自动化平台） |
| **一句话定位** | 面向电机控制工程师的调试与自动化测试上位机，支持多设备类型（电机/温控/电源/传感器/执行器），通过串口通讯实现数据可视化、历史记录、自动化测试与无人值守运行 |
| **软件类型** | 内部工具（不商业化） |
| **用户规模** | 几人小团队 |
| **目标平台** | Windows（主力），后续扩展 Linux |
| **部署方式** | 单机部署 |

### 0.2 用户画像

| 角色 | 技术水平 | 使用频率 | 核心诉求 |
|------|---------|---------|---------|
| **电机控制工程师**（本人） | 专家级 | 每日使用 | 快速调试、直观数据、自动化测试编写 |
| **测试工程师** | 中级 | 按需使用 | 执行自动化测试、查看报告、无人值守 |

### 0.3 核心痛点与价值

| 痛点 | 现状 | 目标 |
|------|------|------|
| 测试费人力 | 需要人工盯着数据、手动记录 | 编写测试流程后自动执行，无人值守 |
| 测试费时间 | 重复性测试占用大量时间 | 自动化测试脚本，一键执行 |
| 数据不直观 | 串口助手+示波器拼凑 | 实时曲线+数值仪表盘+历史回放 |
| 历史数据缺失 | 无法回溯分析 | 自动记录+历史回放+导出 |
| 抖动检测困难 | 人工肉眼判断转速/电流抖动 | 自动化断言：转速波动超阈值自动告警 |

### 0.4 竞品参考

| 软件 | 参考点 | 不参考点 |
|------|--------|---------|
| **VOFA+** | 曲线图形化、实时数据可视化 | — |
| **VESC Tool（本杰明电调）** | 协议驱动架构、参数管理、XML配置 | 与VESC硬件绑定过深 |
| **串口助手+示波器拼凑** | — | 这是要替代的现状 |

**关键设计方向**：曲线图形化参考 VOFA+ 的交互体验，架构参考 VESC Tool 的协议驱动设计。

### 0.5 硬件生态

| 维度 | 决策 |
|------|------|
| **硬件绑定** | 不绑定任何特定MCU/驱动芯片 |
| **通讯方式** | 串口（UART/USB CDC）为主，后续扩展网口（TCP/UDP） |
| **设备类型** | 电机驱动器、温控器（PID温控）、电源/电子负载、传感器采集模块、其他执行器 |
| **通用性** | 设计为通用型上位机，通过可配置的通讯协议适配不同硬件平台 |

### 0.6 核心功能需求（初步）

| 类别 | 功能 | 优先级 |
|------|------|--------|
| 设备连接 | 串口扫描、连接、断开、状态指示 | P0 |
| 实时数据 | 电流/电压/转速/位置/温度/故障 实时显示 | P0 |
| 曲线波形 | 多通道实时曲线，参考VOFA+交互体验 | P0 |
| 数值仪表盘 | 关键变量数值显示 | P1 |
| 参数配置 | 参数读写、导入导出、在线修改 | P0 |
| 自动化测试 | 测试流程编写、执行、断言判断、报告生成 | P0 |
| 历史数据 | 数据记录、回放、导出CSV | P1 |
| 日志系统 | 分级日志、文件保存、UI实时显示 | P0 |
| 故障检测 | 故障码解析、转速/电流抖动自动检测 | P1 |
| 脚本系统 | Lua脚本编写测试流程 | P2 |
| 固件升级 | 串口固件升级 | P2 |
| 多设备 | 同时连接多个设备 | P2 |

### 0.7 待确认事项（后续访谈中逐步明确）

- [x] 电机类型与控制方式（BLDC/PMSM/FOC等） ✅ 第二轮
- [x] 实时数据频率要求 ✅ 第三轮
- [x] 波形系统详细需求 ✅ 第四轮
- [x] 自动化测试详细流程 ✅ 第四轮
- [x] 参数管理详细需求 ✅ 第五轮
- [x] 通讯协议详细设计 ✅ 第五轮
- [x] 插件/脚本/多设备/工程文件/用户权限/日志 ✅ 第六轮
- [x] MVP时间线与里程碑 ✅ 第六轮

### 0.8 电机调试需求（第二轮访谈完成）

| 维度 | 需求 |
|------|------|
| **电机类型** | BLDC（无刷直流）、PMSM（永磁同步） |
| **控制方式** | 方波（六步换向）、FOC（矢量控制） |
| **传感器** | 当前：Hall传感器；后续扩展：无传感器（BEMF观测器）、磁编码器（AS5047/AS5600等） |
| **控制模式** | 三环控制：电流环 → 速度环 → 位置环（可在线切换） |
| **多电机** | 不需要（单电机调试） |

#### 0.8.1 实时变量

- **核心原则**：变量完全开放，类似 VOFA+，用户根据协议自由配置，可增可减
- **预置变量参考**：Ia/Ib/Ic、Id/Iq、Vd/Vq、Vbus、RPM、电角度、机械角度、MOS温度、电机温度、故障码、功率、占空比
- **设计约束**：变量名、数据类型、地址/偏移由用户通过协议配置定义，上位机不做硬编码

#### 0.8.2 参数调试

- **核心原则**：参数类型完全开放，用户根据协议自由增加/删除
- **预置参数参考**：电流环Kp/Ki、速度环Kp/Ki、位置环Kp/Ki、观测器增益、PLL带宽、过流阈值、过压/欠压阈值、温度保护阈值、编码器偏移、Hall偏置、开环启动电流、切换转速
- **设计约束**：参数定义由协议文件驱动，上位机自动生成参数编辑界面

#### 0.8.3 电机参数辨识

- **上位机职责**：仅做辨识过程中的参数显示 + 日志保存
- **辨识逻辑**：在MCU端执行，上位机发指令→收结果
- **辨识项目**：电阻Rs、电感Ld/Lq、磁链、编码器偏移校准、Hall安装角度校准

#### 0.8.4 故障处理

| 故障类型 | 软件调试模式 | 测试模式 |
|----------|------------|---------|
| 过流/短路 | 可继续运行 | 立即停机+报警+等待人工 |
| 过压/欠压 | 可继续运行 | 立即停机+报警+等待人工 |
| 过温 | 可继续运行 | 立即停机+报警+等待人工 |
| 堵转 | 可继续运行 | 立即停机+报警+等待人工 |
| 缺相 | 可继续运行 | 立即停机+报警+等待人工 |
| 编码器故障 | 可继续运行 | 立即停机+报警+等待人工 |
| 通讯超时 | 可继续运行 | 立即停机+报警+等待人工 |

**关键设计**：上位机需要区分"调试模式"和"测试模式"，故障响应策略不同。

#### 0.8.5 操作方式

| 阶段 | 操作方式 |
|------|---------|
| 研发调试 | 手动点击（鼠标操作），支持键盘快捷键 |
| 自动化测试 | 脚本自动运行，无需人工干预 |

### 0.9 实时数据系统（第三轮访谈完成）

#### 0.9.1 数据刷新频率

- **核心原则**：频率由波特率决定，不硬编码固定值
- **典型值**：串口 256000bps → 200~500Hz；USB CDC → 500Hz~1kHz+
- **上位机职责**：连接串口时选择波特率，协议自动解析帧，上位机自动适配帧率
- **设计约束**：上位机不预设固定频率，以实际接收到的帧率驱动刷新

#### 0.9.2 数据通道

- **核心原则**：完全开放，参考 VOFA+，用户自由配置通道数量
- **通道定义**：变量名、数据类型、字节偏移、缩放系数由协议或用户配置决定
- **显示方式**：支持多页面，例如"温度曲线"一个页面、"电机曲线"一个页面，用户自由分页

#### 0.9.3 数据帧格式

- **核心原则**：协议自动解析帧，上位机不关心帧的具体结构
- **解析方式**：协议描述文件定义帧头、帧尾、CRC、变量映射，上位机引擎自动解析
- **支持格式**：float、int16、uint16、int32、uint32 等，参考 VOFA+ 可自由配置

#### 0.9.4 数据精度

- **核心原则**：参考 VOFA+，用户可自由配置数据类型和精度
- **默认**：16位数据（对应12~16位ADC），float 格式
- **显示精度**：用户可配置小数位数

#### 0.9.5 数据保存

| 场景 | 保存策略 |
|------|---------|
| 研发调试（手动） | 不自动保存 |
| 测试模式执行中 | 自动保存 |
| 自动化测试运行中 | 自动保存 |

**设计约束**：保存策略由模式决定，调试模式不自动保存以减少磁盘占用，测试模式必须全量保存。

#### 0.9.6 历史数据回放

- **回放方式**：曲线同步回放，所有数据可观察
- **不需要**：视频播放器式的快进/慢放/暂停/拖动时间轴
- **核心诉求**：能加载历史文件，看到当时的曲线全貌即可

#### 0.9.7 数据导出

- **格式**：CSV
- **附加要求**：导出后可以看到图形化曲线数据（可能附带独立查看器，或导出时附带可视化HTML）

#### 0.9.8 断线重连

- **策略**：重连后弹出提示，用户选择：
  - **继续**：曲线接上继续画（中间可能留空）
  - **不继续**：从头开始新会话

#### 0.9.9 多页面显示

- 不需要多电机，但支持**多页面**（Tab/分页）
- 示例：温度曲线一个页面、电机曲线一个页面、参数仪表盘一个页面
- 用户可自由创建、命名、删除页面

### 0.10 波形系统（第四轮访谈完成）

#### 0.10.1 触发方式

- **不需要触发**，自由运行模式，数据来了就画
- 连接后才开始绘制，断线后停止

#### 0.10.2 数据分析（内置计算引擎）

上位机在兼容流畅性的前提下，尽可能提供详细的数据分析能力：

| 分析功能 | 说明 |
|----------|------|
| 三相电流 RMS | 根据 Ia/Ib/Ic 实时计算均方根值 |
| 峰值/谷值 | 自动追踪并显示 |
| Hall 相位偏差 | 计算 Hall 信号之间的相位偏差 |
| 电流谐波分析 | 可选 FFT，对采样数据做离线频谱分析 |
| 平均值 | 滑动窗口平均值 |
| 功率计算 | 根据 Vbus × I 计算实时功率 |
| 效率计算 | 输入功率 vs 输出功率 |

**设计原则**：计算在后台线程执行，不影响主UI流畅性。尽可能详细，但用户可开关。

#### 0.10.3 游标测量

- 不需要精确游标，大概看个趋势即可
- 自动显示当前值 + 峰值/谷值标记

#### 0.10.4 缓存时间

- 内存中保留**最近15分钟**的实时波形数据
- 超过15分钟滚动覆盖（环形缓冲区）
- 15分钟为默认值，用户可配置

#### 0.10.5 显示性能

- 数据推入频率可能很高（500Hz~1kHz），屏幕刷新60Hz
- 策略：**降采样显示**，但全量数据保留在缓冲区
- 曲线绘制使用 GPU 加速（Qt Quick/QOpenGL）保证流畅

### 0.11 自动化测试（第四轮访谈完成）

#### 0.11.1 测试类型（全部需要）

| 测试类型 | 说明 |
|----------|------|
| 老化测试 | 长时间运行，监测性能衰减趋势 |
| 高速测试 | 额定/最高转速持续运行，监测稳定性 |
| 温升测试 | 运行中监测温度变化曲线，记录温升速率 |
| 效率测试 | 输入功率 vs 输出功率，计算效率曲线 |
| 可靠性测试 | 反复启停/正反转/加载卸载，验证稳定性 |

#### 0.11.2 测试流程定义

- **方式**：表格配置（步骤列表）
- **每步包含**：指令 + 延时 + 判断条件
- 测试工程师无需编程能力，通过表格填写即可定义测试流程
- 复杂场景可扩展为脚本（Lua），但 MVP 阶段以表格为主

#### 0.11.3 判断条件（断言）

- **用户可自行配置**：每个条件可标记为"成功"或"失败"
- 判断条件示例：
  - 转速波动超过 ±X% → 失败
  - 电流超过阈值 → 失败
  - 温度超过阈值 → 失败
  - 故障码出现 → 失败
  - 连续 N 秒数据异常 → 失败
  - 任何可监控变量的条件表达式

#### 0.11.4 测试报告

- **格式**：详细版
- **内容**：每步结果（通过/失败）+ 曲线截图 + 统计数据（均值/峰值/标准差等）
- 报告格式：PDF（优先）或 HTML

#### 0.11.5 异常处理

- 测试过程中出现异常（堵转/过流/过温等）→ **立即通知并报警**
- 硬件故障 → 立即停机 + 报警 + 等待人工处理
- 软件判断失败 → 记录失败，继续执行下一步（用户可配置）

#### 0.11.6 测试序列

- 支持**一次跑完一个流程的所有测试**（全自动）
- 示例流程：老化测试 → 高速测试 → 温升测试 → 效率测试 → 可靠性测试
- 中间无需人工确认，完全无人值守
- 跑完自动生成完整报告

#### 0.11.7 操作模式

| 模式 | 说明 |
|------|------|
| 研发调试 | 手动操作，不自动记录 |
| 测试模式 | 自动化执行，全量数据自动保存，异常立即报警停机 |

### 0.12 参数管理（第五轮访谈完成）

#### 0.12.1 参数分类与职责

| 参数类别 | 上位机职责 | MCU职责 |
|----------|-----------|---------|
| 电机参数（Rs/Ld/Lq/磁链/极对数） | 读写显示 | 存储到Flash |
| PID参数（电流环/速度环/位置环 Kp/Ki/Kd） | 读写显示 | 存储到Flash |
| 保护阈值（过流/过压/欠压/过温） | 读写显示 | 存储到Flash |
| 传感器参数（编码器偏移/Hall偏置/观测器增益） | 读写显示 | 存储到Flash |
| PWM频率/死区时间 | ❌ 不需要 | MCU自行管理 |
| 自动化测试指令参数 | ✅ 上位机保存 | — |

**核心原则**：MCU自己管理参数存储，上位机不做Flash同步。上位机只保存自动化测试指令参数。

#### 0.12.2 工程文件与参数文件

- **不同项目用不同参数文件**
- 上位机本地保存工程文件（.json），包含：
  - 项目名称
  - 串口配置（端口号、波特率等）
  - 协议描述文件引用
  - 自动化测试流程
  - 页面布局配置
  - 自动化测试指令参数
- 参数导出格式：JSON（优先）/ CSV

#### 0.12.3 参数读写

- 在线修改：连上MCU后，读参数→显示→修改→写入→立即生效
- 批量操作：支持"读取全部参数"和"写入全部参数"
- 离线修改：先改好参数文件，连接后一键下发

### 0.13 通讯协议（第五轮访谈完成）

#### 0.13.1 协议模型

- **单向推送 + 命令控制**：MCU主动推送实时数据（保证速度），上位机发控制命令
- 不是主从一问一答，也不是双向自由收发
- 数据通道：MCU → 上位机（单向，高速）
- 命令通道：上位机 → MCU（按需，低速）

#### 0.13.2 协议选型

- **直接使用 VOFA+ 协议**（JustFloat / FireWater 等）
- 上位机兼容 VOFA+ 数据格式，即插即用
- 无需自定义协议描述文件（VOFA+ 协议本身就是标准格式）

#### 0.13.3 命令集

| 命令 | 说明 |
|------|------|
| 电机启停/正反转 | 基本运动控制 |
| 设定转速/电流/位置 | 目标值设定 |
| 控制模式切换 | 电流环/速度环/位置环切换 |
| 参数读/写 | 读取或写入单个/批量参数 |
| 参数保存到Flash | 通知MCU将参数写入Flash |
| 故障复位 | 清除故障状态 |
| 固件升级 | 通过串口升级MCU固件 |
| 电机辨识指令 | 启动Rs/Ld/Lq等辨识流程 |

#### 0.13.4 协议可靠性

- MCU端数据推送：无需应答，丢帧就丢了，保证速度
- 上位机命令：需要超时重试，关键命令（如保存Flash）需要应答确认

### 0.14 软件架构（第六轮访谈完成）

#### 0.14.1 工程文件

- 启动软件直接进入主界面
- 在设置中可导入之前保存的 JSON 工程文件
- 工程文件包含：串口配置、协议描述、页面布局、测试流程、上次连接状态
- 不同项目用不同参数文件，分开显示

#### 0.14.2 多设备连接

- 支持同时连接多个不同类型设备（电机驱动 + 温控器 + 电源 + 传感器）
- 不同设备分不同页面显示，各页面独立
- 每个设备使用独立的串口

#### 0.14.3 日志系统

| 维度 | 需求 |
|------|------|
| 日志触发 | 仅在自动化测试执行时自动保存 |
| 日志内容 | 测试过程中的关键事件 + 数据快照 |
| 保留策略 | 至少保留一次完整脚本测试的日志大小 |
| 日志类型 | 测试日志（核心）+ 系统日志（辅助） |
| 用户操作日志 | 不需要（内部工具，不审计） |
| 通讯日志 | 可选（调试协议时手动开启） |

#### 0.14.4 插件与脚本

- **插件系统**：不需要（内部工具，无需第三方扩展）
- **脚本系统**：不需要（测试流程用表格配置，无需 Lua）
- 设计文档中已有的插件/脚本架构章节保留，作为未来扩展预留

### 0.15 MVP与版本规划（第六轮访谈完成）

#### 0.15.1 MVP功能清单（全部P0）

| 功能 | 归属版本 |
|------|---------|
| 串口连接/断开/状态 | v0.1 |
| 实时曲线（VOFA+协议解析） | v0.1 |
| 多页面（曲线 + 仪表盘） | v0.1 |
| 参数读写 | v0.1 |
| 电机控制命令（启停/正反转/设定值） | v0.1 |
| 故障显示 | v0.1 |
| 数据保存（CSV导出） | v0.1 |
| 自动化测试（表格配置 + 执行 + 报告） | v0.5 |
| 日志系统 | v0.5 |
| 工程文件（JSON导入导出） | v0.1 |

#### 0.15.2 版本路线

| 版本 | 功能 | 里程碑 |
|------|------|--------|
| **v0.1** | 基础调试：连接 + 曲线 + 参数 + 控制 + 故障 + CSV + 工程文件 | 可用的电机调试工具 |
| **v0.5** | 自动化测试：表格配置 + 执行引擎 + 断言 + 报告 + 日志 | 完整自动化测试闭环 |
| **v1.0** | 完整功能：多设备 + 历史回放 + 参数辨识辅助 + 固件升级 + 稳定性 | 生产就绪 |

### 0.16 AI开发策略（第六轮访谈完成）

#### 0.16.1 开发原则

- **完全自动Agent运行**：无需人工控制，AI自主推进
- **按版本完整执行**：v0.1 → v0.5 → v1.0，每个版本独立完成开发+测试+Review
- **人工只在最终阶段微调**：AI完成全部开发后，人工验收和微调
- **多Agent协作**：每个模块独立Agent负责，AI自行定制Agent分工

#### 0.16.2 Git管理

- 使用Git进行版本管理
- 用户已准备好仓库，AI直接使用
- 每个功能模块独立分支，开发完成后合并

#### 0.16.3 项目管理文件

- 全部由AI自动生成和维护
- 目录结构：`.agent/`
  - `PROJECT_STATE.md` — 项目当前状态
  - `TASK_BOARD.md` — 任务看板
  - `CHANGELOG.md` — 变更记录
- AI自动控制上下文，避免上下文丢失

#### 0.16.4 上下文管理

- 每个模块使用独立Agent，避免单Agent上下文溢出
- 主Agent负责协调、合并、Review
- 关键设计决策写入 `.agent/` 目录持久化

### 0.17 波形界面优化需求（第七轮访谈完成）

> 需求变更：对现有波形（曲线）界面进行重构，解决缩放不足与布局不符合使用习惯的问题。

#### 0.17.1 核心问题

- 当前波形无法**完全自由缩放**：X（时间）/ Y（数值）轴需要都能自由缩放
- 当前固定网格布局（2×2 / 3×2 / 4×3）不符合使用习惯

#### 0.17.2 界面布局：单列多行滚动子图

- **放弃固定网格**，改为**单列布局**：一个子图占一行，从上到下排列
- 子图数量超过可视区域时**垂直滚动**（QScrollArea）
- 每个子图是独立的曲线图，**高度可拖动调节**（分隔线拖拽）

#### 0.17.3 子图管理（增删子图）

- 可**自由增加子图**（新增空子图，随后配置显示内容）
- 可**自由删除子图**
- 每个子图可**独立配置显示哪些曲线**（从全部数据通道中选择）
- **同一曲线可同时出现在多个子图**中（例如转速既在"综合图"也在"转速单独图"）
- 支持子图重命名

#### 0.17.4 曲线显示控制

- 每个子图内的每条曲线可**显示/隐藏**切换（仅隐藏不删除）
- 每条曲线可**自由选择颜色**（调色板）
- 曲线名称、单位、数据来源清晰标注
- 子图图例点击可快速显隐曲线

#### 0.17.5 自由缩放（X / Y 轴）

- **X 轴（时间）**：自由缩放、平移
- **Y 轴（数值）**：自由缩放、平移
- 缩放交互方式（综合交互，用户已确认）：

| 手势 | 功能 |
|------|------|
| 滚轮 | X 轴时间缩放 |
| Ctrl + 滚轮 | Y 轴数值缩放 |
| 左键拖拽 | 框选放大区域（rubber-band zoom） |
| 双击 | 自适应（显示全部曲线） |
| 右键拖拽 | 平移 |
| 工具栏 | 放大 / 缩小 / 适应 / 重置 按钮 |

- Y 轴范围支持手动输入（min/max）
- X 轴时间窗长度支持手动输入

#### 0.17.6 时间轴联动（已确认：默认同步 + 可独立）

- **默认所有子图共用一条时间轴**：任一子图缩放/平移，同步子图一起联动
- 每个子图可**单独切换为独立时间轴**（子图右上角开关）
- 同步模式保证子图时间轴严格对齐

#### 0.17.7 优化建议（AI 补充）

- 每个子图**独立 Y 轴自动缩放**（默认自动，可手动锁定范围）
- 支持**保存/恢复布局**：子图集合 + 每子图曲线配置 + 颜色 → 随工程文件持久化
- 保留现有 `CurveManagerPanel` 作为**全局通道管理**（增删通道、统一设色、可见性）
- 子图内曲线显隐与全局面板双向同步
- 缩放时实时显示光标所在点的数据值（tooltip）
- 同一曲线的颜色在所有子图中保持一致（修改一处全局生效）

### 0.18 自动化平台升级需求（第七轮访谈完成）

> 需求变更：自动化界面从"表格步骤"升级为**类似简易代码平台的图形化流程编辑器**，自由度高，按工业自动化标准做全面功能。

#### 0.18.1 定位

- 自动化界面升级为**简易代码平台**：图形化搭建自动化流程，无需编程基础
- 覆盖工业自动化测试/调试的完整操作能力

#### 0.18.2 编辑方式：流程图式（已确认）

- **图形化节点 + 连线**搭建流程（类似 LabVIEW / Scratch）
- 节点**拖拽摆放**，**连线决定执行顺序**
- **判断节点**输出"是 / 否"两条分支路径
- **循环节点**包裹子流程，支持指定次数 / 条件 / 无限（带上限保护）
- 支持**子流程**：把一段流程封装为子图，可复用、可嵌套调用
- 选中节点 → 右侧**参数面板**编辑该节点参数

#### 0.18.3 变量与表达式系统（已确认：完整变量系统）

- **自定义变量**：命名、类型（数值 / 布尔 / 字符串）
- **赋值节点**：把表达式结果或实时通道值赋给变量
- **表达式引擎**：支持四则运算、比较、逻辑（&& / ||）、括号、函数
  - 示例：`avg_current = (Ia + Ib + Ic) / 3`
- **判断条件**使用表达式引擎，可同时引用变量与实时通道值
  - 示例：`温度 < 85 && 转速 > 1400`、`avg_current >= 5.0 || timeout`

#### 0.18.4 节点类型全集（按工业自动化标准）

| 分类 | 节点 | 说明 |
|------|------|------|
| 控制 | 设置参数 | 写参数 / 寄存器 |
| 控制 | 启动电机 | 启动 |
| 控制 | 停止电机 | 停止 / 急停 |
| 控制 | 速度斜坡 | 斜坡调速（起始/目标/加速度） |
| 控制 | 自定义命令 | 发送任意命令帧 |
| 时序 | 延时 | 等待指定毫秒 |
| 时序 | 等待条件 | 等待条件成立（超时可配） |
| 时序 | 定时器 | 计时 / 超时控制 |
| 逻辑 | 判断 if | 条件分支，是/否两条路径 |
| 逻辑 | 循环 loop | for / while / 指定次数 / 无限（带上限） |
| 逻辑 | 分支 switch | 多路分支 |
| 逻辑 | 跳转 | 流程跳转 |
| 数学 | 计算 | 四则运算 / 函数 |
| 数学 | 变量赋值 | 定义并赋值变量 |
| 通信 | 读参数 | 读回设备值 |
| 通信 | 写寄存器 | 直接写寄存器 |
| 数据 | 记录数据 | 记录快照 / 时间段 |
| 数据 | 日志输出 | 写执行日志 |
| 数据 | 导出 | 导出数据 / 报告 |
| 断言 | 断言 | 判定通过 / 失败，输出测试结果 |
| 异常 | 异常处理 | 失败重试 / 跳过 / 报警 / 停机 |
| 流程 | 子流程 | 调用子流程图（复用） |
| 流程 | 注释 | 说明文字（不影响执行） |

#### 0.18.5 判断与执行控制

- 每步（节点）可配置**超时时间**、**失败重试次数**
- 失败处理策略可配置：**继续执行 / 停止 / 报警 / 停机**
- 支持**单步执行**、暂停/继续、停止（调试友好）

#### 0.18.6 运行与报告

- 运行过程中**实时高亮当前执行节点**
- 执行日志实时显示在底部面板
- 测试报告：每节点通过/失败 + 曲线截图 + 统计数据（均值/峰值/标准差）
- 报告格式：PDF（优先）/ HTML / JSON

#### 0.18.7 优化建议（AI 补充）

- 流程图可**保存/加载为 JSON**（作为工程文件的一部分）
- **兼容导入**现有表格型步骤 JSON，自动转换为流程图
- 预留**脚本节点**（Lua / QJSEngine）作为复杂逻辑的逃生舱
- **模板库**：预置老化 / 温升 / 效率 / 可靠性等常用流程，拖入即用
- 节点参数面板按节点类型动态生成表单

---

## 第1章：参考软件设计思想分析

### 1.1 六款参考软件的核心设计哲学

| 参考软件 | 核心设计哲学 | 优先级 | 借鉴方式 |
|----------|-------------|--------|----------|
| **VESC Tool** | 协议驱动架构——通信层与业务层完全解耦 | ★★★★★ | 核心架构借鉴 |
| **ST Motor Pilot** | 状态机驱动的工作流（电机辨识） | ★★★★★ | 流程设计借鉴 |
| **SimpleFOC Studio** | 原子命令 + 序列组合的运动控制模型 | ★★★★ | 命令模型借鉴 |
| **CubeMonitor** | 动态绑定的变量→图表订阅机制 | ★★★★ | 数据绑定借鉴 |
| **LabVIEW** | 硬件抽象层 (HAL) | ★★★★★ | 架构分层借鉴 |
| **TwinCAT/TIA** | Scope 触发 + 预触发缓冲 | ★★★★ | 波形系统借鉴 |

### 1.2 各软件详细分析

#### VESC Tool（Benjamin电调上位机）
- **协议即边界**：通过 VESC Communication Protocol 与固件解耦，上位机与固件可独立演进
- **推模型实时数据**：固件以固定频率主动推送，上位机用环形缓冲区 + 异步刷新消费
- **配置即文件**：所有参数以 XML 持久化，支持导入导出和版本管理
- **不借鉴**：GUI 与逻辑耦合过紧，应采用 MVVM 分离

#### ST Motor Pilot
- **电机辨识作为状态机**：Idle → R测量 → L测量 → 堵转检测 → 开环运行 → 闭环切换
- **仪表盘组件化**：每个组件独立订阅数据源，可自由拖拽组合
- **参数分层**：Profile → Configuration → Register 三级抽象
- **不借鉴**：与 STM32 硬件绑定过深

#### SimpleFOC Studio
- **Web 优先，零安装**：通过 Web Serial API 直接与硬件通信
- **运动控制即命令序列**：抽象为原子命令，支持条件跳转和循环
- **图形化即代码生成器**：WYSIWYG 到代码的映射
- **不借鉴**：架构过于简单，缺乏插件系统

#### CubeMonitor
- **变量监控树**：从 ELF/DWARF 自动提取变量结构
- **实时曲线绑定**：变量→图表动态绑定，运行时可变
- **数据流图**：节点+连线的流式处理范式
- **不借鉴**：ELF 解析依赖特定工具链

#### LabVIEW
- **数据流编程模型**：数据到达即执行，天然并行
- **虚拟仪器 (VI)**：UI + 逻辑一体化封装，高内聚
- **硬件抽象层 (HAL)**：屏蔽底层硬件差异
- **不借鉴**：图形化编程深层次嵌套会变成"面条图"

#### 工业PLC调试软件（TwinCAT / TIA Portal）
- **实时扫描周期可视化**：每个 Cycle 的精确计时
- **Scope 示波器**：触发 + 预触发 + 多通道同步
- **在线修改参数**：双缓冲机制实现原子切换
- **不借鉴**：专有二进制格式不利于分析

---

### 1.3 本章自我Review

**潜在问题：**

1. **问题**：VESC Tool 的推模型假设固件端有稳定的定时推送能力，但实际 MCU 可能因负载波动导致推送间隔不稳定。
   **改进**：在 DataBus 层增加时间戳抖动检测和插值补偿机制。

2. **问题**：ST Motor Pilot 的状态机模型假设所有步骤都是串行的，但实际电机辨识中某些步骤可以并行（如同时测量 R 和 L）。
   **改进**：状态机应支持并行分支和聚合节点。

3. **问题**：CubeMonitor 的 ELF 解析方案依赖固件包含调试信息，这在生产固件中通常不可用。
   **改进**：采用双轨策略——开发阶段用 ELF 解析，生产阶段用 JSON 参数描述文件。

4. **问题**：TwinCAT 的 Scope 触发依赖高速硬件支持，纯软件实现可能无法满足微秒级触发精度。
   **改进**：触发检测在 MCU 端完成，上位机只负责配置触发条件和接收触发后的数据。

5. **问题**：六款参考软件各有侧重，直接拼接可能导致架构"四不像"。
   **改进**：以 VESC Tool 的协议驱动架构为主线，其他作为模块级设计参考。

---

## 第2章：整体目录结构

### 2.1 完整目录树

```
motor_antomation/
│
├── .github/                    # CI/CD 工作流与 PR 模板
│   ├── workflows/
│   │   ├── ci.yml              #   编译 + 单元测试 + 代码检查
│   │   ├── release.yml         #   发布打包 (Windows/Linux)
│   │   └── docs.yml            #   文档自动构建与部署
│   └── ISSUE_TEMPLATE/
│
├── cmake/                      # CMake 构建脚本与模块
│   ├── toolchains/             #   交叉编译工具链文件
│   ├── FindXXX.cmake           #   第三方库查找脚本
│   └── CompilerWarnings.cmake  #   统一编译警告配置
│
├── src/                        # 【核心】应用源代码
│   ├── app/                    #   应用入口与生命周期管理
│   ├── core/                   #   核心业务逻辑（纯 C++，无 UI 依赖）
│   ├── hal/                    #   硬件抽象层（多平台适配）
│   ├── protocol/               #   通信协议实现
│   ├── communication/          #   通信层（Transport 抽象）
│   ├── databus/                #   实时数据总线
│   ├── device/                 #   设备管理
│   ├── parameter/              #   参数管理
│   ├── curve/                  #   曲线引擎
│   ├── recorder/               #   数据记录器
│   ├── logger/                 #   日志系统
│   ├── automation/             #   自动化测试引擎
│   ├── script/                 #   脚本引擎
│   ├── project/                #   项目管理
│   ├── settings/               #   配置管理
│   ├── update/                 #   固件升级
│   ├── ui/                     #   用户界面（MVVM 架构）
│   └── plugins/                #   插件系统
│
├── libs/                       # 【内部库】项目内自研的通用库
│   ├── motorlink/              #   MotorLink 协议库（纯协议编解码）
│   ├── scope/                  #   Scope 数据采集与触发引擎
│   ├── pidtuner/               #   PID 自动整定算法库
│   ├── motorident/             #   电机参数辨识算法库
│   └── tracing/                #   日志与追踪库
│
├── third_party/                # 【第三方】外部依赖
│   ├── spdlog/                 #   日志库
│   ├── nlohmann_json/          #   JSON 解析
│   ├── imgui/                  #   即时模式 GUI
│   ├── implot/                 #   实时图表绘制
│   ├── catch2/                 #   单元测试框架
│   └── asio/                   #   异步网络/串口库
│
├── tests/                      # 【测试】所有测试代码
│   ├── unit/                   #   单元测试（与 src/ 结构镜像）
│   ├── integration/            #   集成测试
│   ├── simulation/             #   仿真测试（模拟硬件）
│   └── fixtures/               #   测试夹具与测试数据
│
├── tools/                      # 【工具】辅助开发与调试工具
│   ├── protocol_analyzer/      #   协议分析器
│   ├── firmware_builder/       #   固件构建与烧录辅助
│   ├── config_migrator/        #   配置文件版本迁移工具
│   └── benchmark/              #   性能基准测试
│
├── docs/                       # 【文档】项目文档
│   ├── architecture/           #   架构设计文档
│   ├── api/                    #   API 参考文档（Doxygen）
│   ├── user_guide/             #   用户手册
│   ├── developer_guide/        #   开发者指南
│   └── protocol/               #   MotorLink 协议规范
│
├── resources/                  # 【资源】非代码静态资源
│   ├── icons/                  #   应用图标
│   ├── themes/                 #   UI 主题文件
│   ├── translations/           #   国际化翻译文件
│   └── defaults/               #   默认配置模板
│
├── examples/                   # 【示例】示例配置与脚本
│   ├── motor_configs/          #   典型电机配置示例
│   └── scripts/                #   自动化脚本示例
│
├── packaging/                  # 【打包】各平台打包配置
│   ├── windows/                #   Windows 安装包
│   ├── macos/                  #   macOS .dmg
│   └── linux/                  #   Linux AppImage/.deb
│
├── .clang-format               # 代码格式化规则
├── .clang-tidy                 # 静态分析规则
├── CMakeLists.txt              # 顶层 CMake 构建文件
└── README.md                   # 项目说明
```

### 2.2 分层架构

```
┌──────────────────────────────────────────┐
│  app/                          ← 组装层   │
├──────────────┬───────────────────────────┤
│     ui/      │      plugins/   ← 表现层   │
├──────────────┴───────────────────────────┤
│           services/             ← 编排层   │
├──────────────┬───────────────────────────┤
│    core/     │    protocol/     ← 领域层   │
├──────────────┴───────────────────────────┤
│              hal/               ← 基础设施层│
└──────────────────────────────────────────┘
```

**依赖规则**：箭头只能从上往下，`core/` 和 `libs/` 不依赖 `ui/`，`hal/` 不依赖任何上层模块。

### 2.3 扩展指南

| 场景 | 放置位置 | 理由 |
|------|---------|------|
| 新增一种电机类型 | `src/plugins/stepper/` | 插件系统，不影响现有代码 |
| 新增一种通信协议 | `src/hal/ethercat/` + `src/protocol/ethercat/` | HAL传输层 + protocol适配 |
| 新增一种 PID 整定算法 | `libs/pidtuner/` | 算法属于内部库 |
| 新增一个仪表盘组件 | `src/ui/components/` | UI 组件目录 |
| 新增一个数据导出格式 | `src/core/export/` | 核心层功能 |

### 2.4 本章自我Review

**潜在问题：**

1. **问题**：`libs/` 和 `src/` 的边界不够清晰，`motorlink` 协议库放在 `libs/` 但 `protocol/` 放在 `src/`。
   **改进**：明确 `libs/` 是纯算法/协议库（可被固件端复用），`src/protocol/` 是 PC 端协议适配层。

2. **问题**：`services/` 目录在 `src/` 下但未在目录树中展开。
   **改进**：`services/` 应作为 `src/` 的子目录，包含 `MotorControlService`、`DataLoggingService`、`FirmwareUpdateService` 等。

3. **问题**：`third_party/` 使用 Git Submodule 可能导致 clone 缓慢。
   **改进**：优先使用 CMake FetchContent，仅在需要精确版本控制时使用 Submodule。

4. **问题**：`tools/` 目录下的工具可能与主项目共享代码但构建系统分离。
   **改进**：每个工具应有独立的 CMakeLists.txt，通过 `add_subdirectory` 可选编译。

5. **问题**：`plugins/` 放在 `src/` 下，但插件可能是独立编译的动态库。
   **改进**：`plugins/` 应提升到顶层，每个插件有独立的 CMake 项目。

6. **问题**：缺少 `cmake/` 下的 `motor_antomationConfig.cmake.in` 用于导出包配置。
   **改进**：增加 CMake 包导出支持，便于第三方插件开发。

---

## 第3章：模块划分

### 3.1 模块总览

| 层级 | 模块 | 职责 | 单例 | 目录 |
|------|------|------|------|------|
| 基础设施 | **Settings** | 全局配置管理，JSON持久化 | 是 | `src/settings/` |
| 基础设施 | **Logger** | 统一日志收集、分级、输出 | 是 | `src/logger/` |
| 基础设施 | **Plugin** | 动态库加载、插件注册、生命周期管理 | 是 | `src/plugins/` |
| 通讯层 | **Communication** | 物理传输通道封装 (Serial/CAN/TCP/UDP) | 是 | `src/communication/` |
| 通讯层 | **Protocol** | 帧解析、校验、分包/组包 | 多实例 | `src/protocol/` |
| 数据层 | **DataBus** | 发布-订阅数据总线，环形缓冲区 | 是 | `src/databus/` |
| 数据层 | **Recorder** | 高速数据记录到磁盘 | 是 | `src/recorder/` |
| 数据层 | **ParameterManager** | 参数读写、缓存、校验、导入导出 | 是 | `src/parameter/` |
| 业务层 | **DeviceManager** | 设备发现、连接、心跳、状态监控 | 是 | `src/device/` |
| 业务层 | **CurveEngine** | 高性能实时曲线渲染 | 多实例 | `src/curve/` |
| 业务层 | **Automation** | 自动化测试序列执行 | 是 | `src/automation/` |
| 业务层 | **ScriptEngine** | 嵌入式脚本运行时 (Lua/Python) | 是 | `src/script/` |
| 业务层 | **Update** | 固件升级 (分块上传/CRC/回滚) | 是 | `src/update/` |
| 应用层 | **ProjectManager** | 项目文件创建/打开/保存/关闭 | 是 | `src/project/` |
| 应用层 | **UI/Core** | MVVM架构的UI核心 | 多实例 | `src/ui/` |

### 3.2 模块依赖关系图

```
                              ┌─────────────┐
                              │  UI / Core   │
                              └──────┬───────┘
                                     │
          ┌──────────────────────────┼──────────────────────────┐
          ▼                          ▼                          ▼
   ┌──────────────┐         ┌──────────────┐          ┌──────────────┐
   │  CurveEngine  │         │  Automation   │          │ProjectManager│
   └──────┬───────┘         └──────┬───────┘          └──────┬───────┘
          │                        │                          │
          │         ┌──────────────┼──────────────┐          │
          │         ▼              ▼              ▼          │
          │  ┌────────────┐ ┌────────────┐ ┌────────────┐    │
          │  │ScriptEngine │ │  Recorder  │ │  Update    │    │
          │  └──────┬─────┘ └──────┬─────┘ └──────┬─────┘    │
          │         │              │              │           │
          ▼         ▼              ▼              ▼           ▼
   ┌─────────────────────────────────────────────────────────────┐
   │                        DataBus                               │
   └──────────────────────────┬──────────────────────────────────┘
                              │
   ┌──────────────────────────┼──────────────────────────────────┐
   │                 ParameterManager                             │
   └──────────────────────────┬──────────────────────────────────┘
                              │
   ┌──────────────────────────┼──────────────────────────────────┐
   │              DeviceManager  │  Protocol  │  Communication    │
   └──────────────────────────┴─────────────┴───────────────────┘
                              │
   ┌──────────────────────────┼──────────────────────────────────┐
   │              Settings  │  Logger  │  Plugin                  │
   └─────────────────────────────────────────────────────────────┘
```

### 3.3 关键模块接口

#### Communication（通信层）

```cpp
class ITransport : public QObject {
    Q_OBJECT
public:
    virtual bool open(const TransportConfig& cfg) = 0;
    virtual void close() = 0;
    virtual bool isOpen() const = 0;
    virtual qint64 send(const QByteArray& data) = 0;
    virtual TransportType type() const = 0;
signals:
    void dataReceived(const QByteArray& data);
    void stateChanged(TransportState state);
    void errorOccurred(TransportError error, const QString& message);
};
```

#### DataBus（数据总线）

```cpp
class DataBus : public QObject {
    Q_OBJECT
public:
    static DataBus* instance();
    template<typename T>
    void publish(const DataTopic<T>& topic, const T& value, qint64 timestamp = 0);
    template<typename T>
    SubscriptionHandle subscribe(const DataTopic<T>& topic,
                                  std::function<void(const T&, qint64)> callback,
                                  Qt::ConnectionType type = Qt::QueuedConnection);
    void unsubscribe(SubscriptionHandle handle);
    template<typename T>
    std::optional<T> snapshot(const DataTopic<T>& topic) const;
};
```

#### ParameterManager（参数管理）

```cpp
class ParameterManager : public QObject {
    Q_OBJECT
public:
    static ParameterManager* instance();
    QFuture<ParamResult> readParam(const QString& paramId);
    QFuture<ParamResult> writeParam(const QString& paramId, const QVariant& value);
    QFuture<BatchResult> readParams(const QStringList& paramIds);
    bool exportToFile(const QString& filePath);
    bool importFromFile(const QString& filePath);
    DiffResult diff(const QString& filePath) const;
signals:
    void paramChanged(const QString& paramId, const QVariant& newValue);
};
```

### 3.4 本章自我Review

**潜在问题：**

1. **问题**：`DataBus` 作为中心节点，如果它成为性能瓶颈，整个系统都会受影响。
   **改进**：设计时应确保 `publish()` 是 O(1) 操作，订阅匹配使用哈希表而非遍历。

2. **问题**：`ParameterManager` 依赖 `Protocol` 层，但 `Protocol` 又依赖 `Communication`，形成长依赖链。
   **改进**：`ParameterManager` 应依赖 `IParameterProvider` 抽象接口，而非直接依赖 `Protocol`。

3. **问题**：`CurveEngine` 被设计为多实例，但底层共享 `DataBus` 订阅，可能导致订阅管理混乱。
   **改进**：`CurveEngine` 内部使用 `CurveModel` 聚合订阅，每个 `CurveModel` 实例管理自己的订阅生命周期。

4. **问题**：`Automation` 和 `ScriptEngine` 功能重叠（都能执行自动化任务）。
   **改进**：明确 `Automation` 是高层编排引擎，`ScriptEngine` 是底层执行引擎，`Automation` 可以调用 `ScriptEngine`。

5. **问题**：`Plugin` 模块与 `Communication` 模块存在循环依赖风险（插件需要通信，通信需要加载插件）。
   **改进**：`Plugin` 模块只提供加载机制，不依赖任何业务模块；业务模块通过注册表查找插件。

6. **问题**：缺少模块间的版本兼容性检查机制。
   **改进**：每个模块导出 `ModuleVersion` 结构体，启动时由 `App` 层统一校验。

---

## 第4章：通讯架构

### 4.1 分层架构

```
┌─────────────────────────────────────────────────────────────┐
│                      Command Layer                           │
│  命令队列 (优先级) | 请求-响应匹配 (seqId) | 超时/重试         │
│  Promise/Future 异步模式 | RequestMatcher                    │
├─────────────────────────────────────────────────────────────┤
│                      Stream Layer                            │
│  实时数据流 | 环形缓冲区 (RingBuffer) | 批量发布               │
│  StreamParser | StreamBuffer                                 │
├─────────────────────────────────────────────────────────────┤
│                      Protocol Layer                          │
│  帧解析状态机 | CRC16 校验 | 分包/组包                        │
│  FrameCodec | IProtocol 抽象接口                             │
├─────────────────────────────────────────────────────────────┤
│                      Transport Layer                         │
│  Serial | USB CDC | CAN | TCP | UDP                          │
│  ITransport 抽象接口 | TransportFactory                      │
└─────────────────────────────────────────────────────────────┘
```

### 4.2 核心设计原则

**完全异步，零同步等待**：

```cpp
// ❌ 反模式：同步等待
auto response = transport->sendSync(data);  // 线程阻塞！

// ✅ 正确：异步 Promise/Future
QFuture<CommandResponse> future = commandQueue->execute(request);
auto watcher = new QFutureWatcher<CommandResponse>();
connect(watcher, &QFutureWatcher<CommandResponse>::finished, [=]() {
    auto response = watcher->result();
    // 处理响应
});
watcher->setFuture(future);
```

### 4.3 帧结构设计

```
[STX:2B] [LEN:2B] [SEQ:2B] [CMD:2B] [PAYLOAD:NB] [CRC16:2B] [ETX:2B]
 0xAA55   长度     序列号   命令码    有效载荷      CRC16校验   0x55AA
```

- 命令帧 (CMD 0x0000-0x7FFF)：请求-响应模式
- 流数据帧 (CMD 0x8000-0xFFFF)：实时数据推送

### 4.4 数据流全景

**上行数据流（设备 → 软件）**：

```
物理设备 → Transport.readyRead → Protocol.feed() → FrameCodec状态机
  ├── commandDecoded → RequestMatcher(seqId匹配) → CommandPromise.resolve()
  └── streamDataDecoded → StreamParser → DataBus.publish()
      ├── CurveEngine (波形渲染)
      ├── Recorder (数据记录)
      └── UI (仪表盘更新)
```

**下行数据流（软件 → 设备）**：

```
UI/Automation → CommandQueue.execute(request)
  → 生成seqId → 入优先级队列 → IProtocol.encode() → ITransport.send()
  → 物理设备 → 响应 → Transport.readyRead → Protocol.feed()
  → RequestMatcher.match(seqId) → CommandPromise.resolve()
```

### 4.5 新增通信方式（以 SPI 为例）

只需三步，无需修改任何现有代码：

```cpp
// 步骤1：实现 ITransport 接口
class SpiTransport : public ITransport { /* ... */ };

// 步骤2：注册到工厂
TransportFactory::registerTransport(TransportType::SPI, []() {
    return new SpiTransport();
});

// 步骤3：注册到 CommunicationManager
auto* spi = TransportFactory::create(TransportType::SPI);
CommunicationManager::instance()->registerTransport(spi);
```

### 4.6 本章自我Review

**潜在问题：**

1. **问题**：`QFuture` 不支持真正的链式调用 `.then()`，必须使用 `QFutureWatcher`，代码冗长。
   **改进**：在 Qt6 中 `QFuture` 已支持 `.then()`，应使用 Qt6 的链式调用特性。

2. **问题**：`RequestMatcher` 使用 `QHash<quint16, CommandPromise*>` 存储，seqId 只有 16 位，可能在高并发下快速回绕。
   **改进**：seqId 扩展为 32 位，并在回绕时检查是否有未完成的旧请求。

3. **问题**：帧结构的 STX (0xAA55) 和 ETX (0x55AA) 可能在 payload 中出现，导致帧同步错误。
   **改进**：使用 COBS (Consistent Overhead Byte Stuffing) 或 SLIP 编码对 payload 进行转义。

4. **问题**：`StreamParser` 和 `CommandQueue` 共享同一个 `IProtocol` 实例，但流数据和命令数据的处理优先级不同。
   **改进**：在 `IProtocol` 内部使用两条独立的解码路径，流数据帧直接跳过命令队列。

5. **问题**：缺少流量控制机制，如果 MCU 发送速率超过 PC 处理能力，会导致数据丢失。
   **改进**：在 Transport 层实现硬件流控 (RTS/CTS) 或协议层实现软件流控 (XON/XOFF)。

6. **问题**：`CommunicationManager` 作为单例管理所有 Transport，但多设备场景下可能需要多个 Transport 同时工作。
   **改进**：`CommunicationManager` 应支持多 Transport 并发管理，而非仅 `activeTransport`。

---

## 第5章：实时数据总线

### 5.1 核心设计

```
MCU 上传原始数据
       │
       ▼
   Transport 层 → Protocol 层 → DataBus.publish(topic, dataPoint)
       │
       ├──→ CurveEngine  (更新波形缓存)
       ├──→ Logger       (持久化记录)
       ├──→ Recorder     (数据录制)
       ├──→ Automation   (条件判断)
       └──→ UI           (仪表盘更新)
```

### 5.2 数据结构

```cpp
struct DataPoint {
    uint64_t    timestamp;   // 微秒级时间戳
    uint8_t     deviceId;    // 设备ID
    uint8_t     motorId;     // 电机ID
    uint16_t    variableId;  // 变量ID
    ValueType   value;       // 类型安全 variant
    Quality     quality;     // 数据质量 (Good/Uncertain/Bad/Stale)
    uint32_t    sequence;    // 序列号（丢包检测）
};
```

### 5.3 Topic 命名规范

```
格式: device.{deviceId}.motor.{motorId}.{variableName}

示例:
  device.1.motor.1.iq              // 设备1电机1的Iq电流
  device.1.motor.1.speed           // 设备1电机1的转速
  device.1.motor.1.temp_mos        // 设备1电机1的MOS温度

通配符:
  device.*.motor.*.iq              // 所有设备所有电机的Iq
  device.1.motor.1.*               // 设备1电机1的全部变量
```

### 5.4 环形缓冲区（无锁设计）

```cpp
template<typename T, size_t Capacity>
class RingBuffer {
    // Capacity 必须为2的幂，用位运算取模
    // 单生产者/单消费者：无锁安全
    // 多生产者：需扩展为 CAS 操作
    bool push(const T& item);       // 非阻塞写入
    std::optional<T> pop();         // 非阻塞读取
    size_t readBulk(T* out, size_t maxCount);  // 批量读取（零拷贝）
};
```

### 5.5 Pub/Sub 接口

```cpp
class IDataBus {
public:
    virtual uint64_t subscribe(const std::string& topicPattern,
                               DataCallback callback) = 0;
    virtual void unsubscribe(uint64_t subscriptionId) = 0;
    virtual void publish(const std::string& topic, const DataPoint& data) = 0;
    virtual void publishBatch(const std::vector<std::pair<std::string, DataPoint>>& batch) = 0;
    virtual std::optional<DataPoint> getLatest(const std::string& topic) = 0;
    virtual std::vector<DataPoint> getHistory(const std::string& topic,
                                               uint64_t fromUs, uint64_t toUs,
                                               size_t maxPoints = 10000) = 0;
};
```

### 5.6 性能目标

| 指标 | 目标 |
|------|------|
| 支持变量数 | 100+ |
| 更新频率 | 1kHz |
| 延迟 | < 1ms |
| 内存拷贝 | 零拷贝（Zero-copy） |
| 订阅匹配 | O(1) 哈希查找 |

### 5.7 本章自我Review

**潜在问题：**

1. **问题**：通配符匹配 `device.*.motor.*.iq` 使用字符串遍历，在大规模订阅时可能退化为 O(N*M)。
   **改进**：使用 Trie 树索引 Topic，将通配符匹配优化到 O(层级数)。

2. **问题**：`getHistory()` 需要遍历整个环形缓冲区，在缓冲区较大时（如 100万点）可能耗时较长。
   **改进**：为历史数据增加时间范围索引（如每 1000 点一个摘要索引）。

3. **问题**：`DataPoint` 使用 `std::variant`，每次发布都涉及类型擦除和恢复的开销。
   **改进**：对于高频数据（如 Ia, Ib, Ic），使用固定类型的专用通道，避免 variant 开销。

4. **问题**：`RingBuffer` 在满时覆盖最旧数据，可能导致消费者丢失数据而不知情。
   **改进**：增加 `sequence` 字段，消费者可通过 sequence 间隙检测数据丢失。

5. **问题**：`publishBatch()` 在持有锁期间执行所有订阅回调，如果某个回调执行缓慢会阻塞所有其他订阅者。
   **改进**：将回调分发到独立线程池，或使用 `Qt::QueuedConnection` 将回调异步化。

6. **问题**：Topic 字符串 `"device.1.motor.1.iq"` 每次 publish 都需要构造，产生大量内存分配。
   **改进**：使用预注册的 Topic ID（整数）替代字符串，或在 Topic 内部使用 `string_view`。

---

## 第6章：波形系统

### 6.1 核心架构

```
┌─────────────────────────────────────────────────────────────┐
│                      CurveWidget (QGraphicsView)             │
│  前端渲染层：QGraphicsScene + 增量绘制                        │
├─────────────────────────────────────────────────────────────┤
│                      CurveEngine (纯数据，无UI依赖)           │
│  CurveModel (单变量时间序列) | CurveCollection (多变量管理)     │
│  LTTB 降采样 | 分级缓存 (热/冷) | 多窗口同步                   │
├─────────────────────────────────────────────────────────────┤
│                      DataBus                                 │
│  订阅 device.*.motor.*.* 实时数据                             │
└─────────────────────────────────────────────────────────────┘
```

### 6.2 核心功能

| 功能 | 说明 |
|------|------|
| 100+ 变量同时显示 | 多通道叠加，独立 Y 轴缩放 |
| 暂停/继续 | 冻结/恢复波形滚动 |
| 缩放 (X/Y 独立) | 滚轮缩放 + 拖拽平移 |
| 历史缓存 | 环形缓冲区，可配置大小 (默认 10,000 点/通道) |
| Marker (标记点) | 用户手动标记关键事件 |
| Cursor (测量光标) | 双光标，显示 ΔX, ΔY, dY/dX |
| FFT (预留) | 接口预留，后期实现频域分析 |
| 导出 CSV | 选定时间范围导出 |
| 截图 | 高分辨率波形截图 |
| 多窗口同步 | 同一时间轴，多窗口联动 |

### 6.3 降采样算法 (LTTB)

Largest Triangle Three Buckets：将 N 个数据点降采样到 M 个点，保留视觉特征。

```
复杂度: O(N)
适用场景: 100万点 → 1000点 (屏幕宽度)
```

### 6.4 分级缓存设计

```
┌──────────────┐     ┌──────────────┐     ┌──────────────┐
│  GPU 缓存     │ ←── │  RAM 热缓存   │ ←── │  磁盘冷缓存   │
│  (当前窗口)   │     │  (最近10分钟) │     │  (全部历史)   │
│  < 1000点    │     │  < 100万点   │     │  无限制       │
└──────────────┘     └──────────────┘     └──────────────┘
```

### 6.5 本章自我Review

**潜在问题：**

1. **问题**：LTTB 降采样在每帧都需要执行，如果原始数据量大（100万点），仍可能达到 10ms+ 的计算时间。
   **改进**：LTTB 结果应缓存，仅在数据范围变化时重新计算。

2. **问题**：QGraphicsView 在 100+ 通道时，场景管理开销显著增加。
   **改进**：使用 OpenGL 后端 (`QOpenGLWidget` viewport) 加速渲染。

3. **问题**：多窗口同步需要共享时间轴状态，如果使用信号/槽同步，可能产生事件风暴。
   **改进**：使用共享的 `TimeRange` 对象，通过原子操作或读写锁同步。

4. **问题**：Cursor 测量需要精确知道每个数据点的原始值，但降采样后的点可能不精确对应。
   **改进**：Cursor 查询时应回退到原始数据，而非降采样数据。

5. **问题**：FFT 接口预留但未定义数据格式，后期集成可能不兼容。
   **改进**：现在定义 `FFTResult` 结构体，包含 `frequencies[]` 和 `magnitudes[]`。

6. **问题**：CSV 导出大量数据时可能阻塞 UI 线程。
   **改进**：导出操作在后台线程执行，通过 `QFuture` 异步返回结果。

### 6.6 新版布局与交互需求（第七轮访谈需求变更）

> 对应需求：`0.17 波形界面优化需求`。本小节将新需求落到波形系统的架构设计。

#### 6.6.1 布局：单列多行滚动子图列表

- 用 `VerticalPlotList`（QScrollArea + QVBoxLayout）取代 `MultiCurveContainer` 的固定 Grid/Tab 布局
- 每一行是一个 `PlotCell`（独立 CurveWidget），支持拖动调整高度
- 子图增删通过界面按钮或右键菜单

#### 6.6.2 子图与曲线的配置模型

```
┌──────────────────────────────────────────────────────────┐
│ VerticalPlotList (QScrollArea)                            │
│  ┌────────────────────────────────────────────────────┐  │
│  │ PlotCell #1 [电机转速]                             │  │
│  │   CurveWidget: channels = {Speed, SpeedRef}         │  │
│  │   时间轴: 同步 | 独立(开关)                          │  │
│  └────────────────────────────────────────────────────┘  │
│  ┌────────────────────────────────────────────────────┐  │
│  │ PlotCell #2 [三相电流]                             │  │
│  │   CurveWidget: channels = {Ia, Ib, Ic}              │  │
│  └────────────────────────────────────────────────────┘  │
│  [＋ 新增子图]                                           │
└──────────────────────────────────────────────────────────┘
```

- **PlotLayout 模型**（可序列化 JSON）：
  ```json
  {
    "plots": [
      { "name": "电机转速", "timeAxis": "sync",
        "channels": [ {"topicId": 1, "visible": true, "color": "#00BFFF"},
                      {"topicId": 2, "visible": true, "color": "#FFA500"} ] },
      { "name": "三相电流", "timeAxis": "independent", ... }
    ]
  }
  ```
- 每个 `PlotCell` 维护自己的曲线集合（`QList<PlotChannel>`：topicId + visible + color）
- 同一通道可被多个 PlotCell 引用；**颜色全局唯一**（`TopicRegistry` 统一管理，一处修改全局生效）

#### 6.6.3 缩放交互（综合交互）

- **X 轴（时间）**：滚轮缩放、右键拖拽平移、框选水平范围
- **Y 轴（数值）**：Ctrl+滚轮缩放、框选垂直范围、手动输入 min/max
- **框选放大**：左键拖拽出矩形 → 双击确认放大到该区域（Esc 取消）
- **自适应**：双击 / 工具栏"适应" → 按可见曲线自动计算 X/Y 范围
- 工具栏：放大、缩小、适应、重置、X 范围(s)、Y 范围(min~max)

#### 6.6.4 时间轴联动（默认同步 + 可独立）

- 引入 `TimeAxisManager`：
  - 维护共享 `TimeRange`（t0, xRangeSeconds），原子更新，避免信号风暴
  - 每个 PlotCell 有 `sync` 标志；`sync=true` 时缩放/平移写入共享 TimeRange，`sync=false` 时使用自己的 TimeRange
- 同步源：任一同步子图操作 → 广播给所有同步子图；独立子图不受影响

### 6.7 波形系统架构调整方向

| 组件 | 现有 | 调整后 |
|------|------|--------|
| 多图容器 | `MultiCurveContainer`（Tab/Grid） | `VerticalPlotList`（单列滚动） |
| 单图 | `CurveWidget` | `CurveWidget` 保留，增加框选缩放/同步开关 |
| 通道配置 | `CurveManagerPanel`（全局表） | 保留全局表 + 每子图内联配置 |
| 时间轴 | 各窗口独立/Grid 内联动 | `TimeAxisManager` 统一管理 |
| 布局持久化 | 无 | `PlotLayout` 模型序列化到工程文件 |
| 颜色管理 | 每图独立 | `TopicRegistry` 全局统一 |

- 渲染性能约束不变：LTTB 降采样 + OpenGL 后端，100+ 通道仍流畅

---

## 第7章：参数系统

### 7.1 参数类型支持

| 类型 | 字节数 | 控件 | 示例 |
|------|--------|------|------|
| Bool | 1 | QCheckBox | 使能/禁用 |
| Int8/16/32 | 1/2/4 | QSpinBox | 地址、ID |
| UInt8/16/32 | 1/2/4 | QSpinBox | 故障码 |
| Float32 | 4 | QDoubleSpinBox | PID 增益 |
| Enum | 1-2 | QComboBox | 控制模式 |
| BitField | 1-4 | BitFieldPanel | 故障标志 |
| String | 变长 | QLineEdit | 固件版本 |

### 7.2 参数描述文件 (JSON)

```json
{
    "meta": {
        "device": "MotorController_V2",
        "firmware_version": "2.1.0",
        "param_version": 3
    },
    "groups": [
        {
            "name": "Motor",
            "displayName": "电机配置",
            "subgroups": [
                { "name": "Control/PID", "displayName": "PID控制" },
                { "name": "Protection/Temperature", "displayName": "温度保护" }
            ]
        }
    ],
    "parameters": [
        {
            "id": 1001,
            "name": "motor_current_limit",
            "displayName": "电机电流限制",
            "address": 256,
            "type": "Float32",
            "byteSize": 4,
            "minValue": 0.0,
            "maxValue": 200.0,
            "defaultValue": 50.0,
            "step": 0.1,
            "unit": "A",
            "access": "ReadWrite",
            "group": "Motor",
            "subgroup": "Protection/Current"
        }
    ]
}
```

### 7.3 核心功能

| 功能 | 说明 |
|------|------|
| 单参数读写 | 通过 `paramId` 读写单个参数 |
| 批量读取 | 按分组批量读取 |
| 批量下载 | 全部参数一次性写入 MCU Flash |
| 导入/导出 | JSON/YAML/CSV 格式 |
| 参数比较 | 两个参数集 diff |
| 搜索/过滤 | 关键字搜索、分组过滤、修改过滤 |
| UI 自动生成 | 根据参数类型自动生成对应控件 |
| 参数变更通知 | `paramChanged` 信号 |

### 7.4 缓存策略

```
L1: 值缓存 (valueCache_) —— 读写锁保护，O(1) 查询
    TTL: 无过期（手动 invalidate）
    ↓ miss
L2: IParameterProvider (MCU/协议层)
    readParam() → 通信 → MCU RAM
```

### 7.5 本章自我Review

**潜在问题：**

1. **问题**：参数描述文件与固件参数定义可能不同步，导致上位机显示错误。
   **改进**：增加 `param_version` 字段，启动时校验版本一致性，不一致时提示用户更新。

2. **问题**：`Float32` 的 `step` 和显示精度在 JSON 中未定义，可能导致编辑体验不一致。
   **改进**：增加 `precision` 字段（小数位数）和 `displayFormat` 字段。

3. **问题**：批量下载全部参数到 MCU Flash 时，如果中途失败，Flash 可能处于不一致状态。
   **改进**：使用双区备份 (A/B partition) 机制，下载失败可回滚。

4. **问题**：`BitField` 类型在 JSON 中定义复杂，且跨语言解析可能不一致。
   **改进**：使用位掩码 (bitmask) 的整数表示，配合 `bitFields` 数组描述每个位的含义。

5. **问题**：参数 `address` 使用绝对地址，但不同固件版本地址可能变化。
   **改进**：使用逻辑 ID (`paramId`) 作为主键，`address` 仅作为传输时的映射。

6. **问题**：UI 自动生成的控件布局可能不够灵活，无法满足复杂布局需求。
   **改进**：支持自定义布局模板，允许用户覆盖默认的自动生成布局。

---

## 第8章：自动化测试框架

### 8.1 核心架构

```
┌─────────────────────────────────────────────────────────────┐
│                  Automation Framework                        │
│                                                             │
│  JSON/YAML定义 | UI拖拽创建 | 脚本定义 | Plugin扩展            │
│        │             │          │          │                │
│        └─────────────┴────┬─────┴──────────┘                │
│                           ▼                                  │
│                   TestDefinition                             │
│                    (统一中间表示)                              │
│                           ▼                                  │
│  ┌─────────────────────────────────────────────────────┐    │
│  │                    TestRunner                        │    │
│  │  责任链(步骤调度) | 命令模式(Step执行)                  │    │
│  │  策略模式(断言切换) | 观察者模式(事件通知)              │    │
│  └──────────────────────┬──────────────────────────────┘    │
│                         ▼                                    │
│  ┌─────────────────────────────────────────────────────┐    │
│  │                   TestContext                        │    │
│  │  参数存储 | 数据记录 | 变量作用域 | 设备引用            │    │
│  └──────────────────────┬──────────────────────────────┘    │
│                         ▼                                    │
│  ┌─────────────────────────────────────────────────────┐    │
│  │                   TestReport                         │    │
│  │  详细报告 | 汇总报告 | PDF导出 | HTML/JSON             │    │
│  └─────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────┘
```

### 8.2 核心接口

```cpp
// 命令模式：每个步骤封装为独立命令
class ITestStep {
public:
    virtual StepResult execute(TestContext& context) = 0;
    virtual StepResult undo(TestContext& context) { /* 默认空实现 */ }
    virtual std::string id() const = 0;
    virtual std::string type() const = 0;  // "SetParameter", "WaitFor" 等
    virtual int timeoutMs() const { return 30000; }
    virtual int maxRetries() const { return 0; }
    virtual bool continueOnFailure() const { return false; }
};

// 策略模式：断言策略可独立替换
class IAssertion {
public:
    virtual AssertionResult evaluate(const TestContext& context) = 0;
};

// 工厂模式：支持插件扩展
class StepFactory {
public:
    void registerStep(const std::string& type, Creator creator);
    std::unique_ptr<ITestStep> create(const std::string& type);
};
```

### 8.3 内置步骤类型

| 步骤类型 | 说明 |
|----------|------|
| SetParameter | 设置参数值 |
| WaitFor | 等待条件满足 |
| RecordData | 记录数据点 |
| AssertCondition | 断言判断 |
| RampSpeed | 斜坡调速 |
| SendCommand | 发送命令 |
| Delay | 延时 |
| Loop | 循环 |
| Branch | 条件分支 |
| SubTest | 调用子测试 |

### 8.4 测试定义示例 (JSON)

```json
{
    "testId": "motor_speed_ramp_test",
    "testName": "电机速度斜坡测试",
    "steps": [
        { "type": "SetParameter", "paramId": "control_mode", "value": 2 },
        { "type": "SetParameter", "paramId": "target_speed", "value": 3000 },
        { "type": "Delay", "ms": 500 },
        { "type": "RecordData", "label": "speed_after_ramp" },
        { "type": "AssertCondition", "expression": "motor.speed >= 2900 && motor.speed <= 3100" }
    ]
}
```

### 8.5 本章自我Review

**潜在问题：**

1. **问题**：`ITestStep` 的 `undo()` 方法是可选的，但某些步骤（如 `SetParameter`）需要 undo 来回滚。
   **改进**：将 `SetParameter` 步骤的原始值自动保存在 `TestContext` 中，`undo()` 时自动恢复。

2. **问题**：`Loop` 步骤可能导致无限循环，缺乏超时保护。
   **改进**：`Loop` 步骤必须指定 `maxIterations` 和 `timeoutMinutes`。

3. **问题**：`AssertCondition` 使用字符串表达式，需要运行时解析，性能和安全性都有风险。
   **改进**：使用预编译的表达式树，或使用 Lua 脚本进行条件求值。

4. **问题**：JSON 定义测试不够直观，非开发人员难以编写。
   **改进**：提供可视化的拖拽式测试编辑器，自动生成 JSON。

5. **问题**：`TestContext` 在步骤间共享状态，但缺乏事务性保证——如果步骤 3 失败，步骤 1 和 2 的副作用可能未被清理。
   **改进**：引入 `TestTransaction` 概念，支持 `commit()` 和 `rollback()`。

6. **问题**：`StepFactory` 的注册是静态的，但插件可能动态加载，注册时机不确定。
   **改进**：`StepFactory` 使用线程安全的注册表，支持运行时动态注册。

### 8.6 新版自动化平台需求（第七轮访谈需求变更）

> 对应需求：`0.18 自动化平台升级需求`。编辑范式从"表格步骤"升级为**流程图式简易代码平台**，并引入**完整变量与表达式系统**。

#### 8.6.1 数据模型：FlowGraph（统一中间表示）

```
┌──────────────────────────────────────────────────────────────┐
│                    FlowGraph（数据模型）                        │
│  ┌──────────┐      ┌──────────┐      ┌──────────┐           │
│  │ FlowNode │─────▶│ FlowNode │─────▶│ FlowNode │           │
│  │ 设置参数  │ 边   │  延时    │ 边   │  判断    │           │
│  └──────────┘      └──────────┘      └────┬─┬───┘           │
│                                         是 │ │ 否            │
│                                  ┌────────┘ └───────┐        │
│                             (各分支子流程节点)          │        │
└──────────────────────────────────────────────────────────────┘
```

- **FlowNode**：节点，含 `type`、`params`（键值）、`id`
- **FlowEdge**：有向边，连接节点输出端口 → 输入端口；判断节点有"是/否"两个输出端口
- **FlowSubGraph**：子流程节点，内含独立 FlowGraph，可嵌套、复用
- **FlowGraph 序列化**：JSON（节点 + 连线 + 变量声明 + 元数据），工程文件的一部分
- **兼容旧格式**：旧表格 JSON 步骤可自动转换为 FlowGraph（顺序连成链）

#### 8.6.2 编辑器：FlowCanvas

- `FlowCanvas`（QGraphicsView/Scene）：拖拽摆放节点、连线、框选、删除
- 节点库面板：按分类列出全部节点类型（见 `0.18.4` 节点全集）
- 节点参数面板：选中节点 → 右侧动态表单编辑参数（按类型生成）
- 运行态：当前执行节点高亮、断点（可选）、单步

#### 8.6.3 变量与表达式引擎

- **VariableScope**：变量表（名称 + 类型 + 当前值），贯穿整个流程执行
- **ExpressionEngine**：解析并求值表达式字符串
  - 支持：四则运算、比较、逻辑（&& / ||）、括号、函数（min/max/avg/abs…）
  - 引用：变量名、实时通道值（`channel:Speed`）、常量
  - 求值安全：预编译表达式树 + 求值超时，避免死循环/注入
- 示例表达式：`(channel:Ia + channel:Ib + channel:Ic) / 3 > 10`
- 判断节点与"等待条件"节点的条件都走 ExpressionEngine

#### 8.6.4 节点执行：FlowRunner（替换/增强 TestRunner）

- **FlowRunner** 沿 FlowGraph 拓扑执行，处理分支、循环、跳转
- 节点执行统一接口：`execute(ExecutionContext& ctx)`（Command 模式，保留现有 `ITestStep` 语义）
- **ExecutionContext**：变量表 + 设备引用 + 日志 + 数据记录 + 报告写入
- 失败策略：每个节点可配置 `onFailure = continue | stop | alarm | shutdown`
- 循环上限保护：`maxIterations` + `timeoutMinutes`（强制）
- 线程安全：执行在工作线程，UI 通过信号/槽接收进度（保留现有线程模型）

#### 8.6.5 节点类型清单（落地为内置 FlowNode）

覆盖 `0.18.4` 全集：控制（设置参数/启停电机/速度斜坡/自定义命令）、时序（延时/等待条件/定时器）、逻辑（if/循环/switch/跳转）、数学（计算/赋值）、通信（读参数/写寄存器）、数据（记录/日志/导出）、断言、异常处理、子流程、注释。

#### 8.6.6 与现有组件的关系

| 组件 | 调整 |
|------|------|
| `AutomationEngine` | 保留，扩展为可执行 FlowGraph（新增 `loadFlowGraph()`） |
| `TestRunner` | 保留表格执行路径，新增 FlowRunner 路径；两者共用 StepFactory/断言 |
| `AutomationWidget` | 表格视图升级为 FlowCanvas 编辑器 + 运行控制 + 日志/报告面板 |
| `ReportGenerator` | 沿用，报告内容增加流程图快照 |
| 脚本系统（第9章） | FlowGraph 预留脚本节点，绑定脚本 API（互不冲突） |

---

## 第9章：脚本系统

### 9.1 架构设计

```
┌─────────────────────────────────────────────────────────────┐
│                     ScriptEngine                             │
│                                                             │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │  ScriptHost   │  │ScriptBinding  │  │ ScriptSandbox │      │
│  │  (生命周期)   │  │ (C++ API暴露) │  │  (安全隔离)   │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
│                                                             │
│  ┌──────────────┐  ┌──────────────┐                        │
│  │  Lua Engine   │  │ Python Engine │  (可替换)             │
│  │  (sol2/sol3)  │  │  (pybind11)   │                        │
│  └──────────────┘  └──────────────┘                        │
└─────────────────────────────────────────────────────────────┘
```

### 9.2 脚本 API 绑定

```lua
-- Lua 脚本示例：电机速度斜坡测试
motor.setTargetSpeed(3000)
sleep(500)
local speed = motor.getActualSpeed()
assert(math.abs(speed - 3000) < 100, "速度偏差过大")
logger.info("速度测试通过: " .. speed .. " RPM")
```

暴露的 API 模块：
- `motor`：电机控制 (setSpeed, getSpeed, enable, disable, getParameter)
- `databus`：数据订阅 (subscribe, getLatest)
- `logger`：日志 (info, warn, error)
- `automation`：自动化 (run, assert)
- `sleep`：延时

### 9.3 安全设计

| 机制 | 说明 |
|------|------|
| 执行超时 | 默认 30s，可配置 |
| 内存限制 | 限制脚本内存使用 |
| API 白名单 | 仅暴露安全 API |
| 文件系统隔离 | 默认禁止文件访问 |
| 沙箱隔离 | 可选独立进程执行 |

### 9.4 本章自我Review

**潜在问题：**

1. **问题**：Lua 和 Python 双引擎维护成本高，API 需要在两套绑定中分别实现。
   **改进**：第一阶段只实现 Lua，第二阶段再评估是否需要 Python。

2. **问题**：`sleep()` 在脚本中是阻塞调用，如果在主线程执行会冻结 UI。
   **改进**：脚本始终在后台线程执行，`sleep()` 使用协程或 `QTimer` 实现。

3. **问题**：脚本 API 直接暴露 `motor.setTargetSpeed()`，缺乏权限控制。
   **改进**：增加 API 权限级别，敏感操作需要用户确认。

4. **问题**：脚本错误（如 `assert` 失败）的堆栈信息可能不够清晰。
   **改进**：在脚本引擎中捕获错误并转换为结构化的错误报告。

5. **问题**：`ScriptSandbox` 的独立进程模式增加了 IPC 通信开销。
   **改进**：默认使用线程级沙箱，仅在需要严格隔离时启用进程级沙箱。

6. **问题**：脚本间缺乏依赖管理，如果脚本 A 依赖脚本 B 的函数，无法自动加载。
   **改进**：增加 `require()` 或 `import()` 机制，支持脚本模块化。

---

## 第10章：插件系统

### 10.1 插件类型

| 插件类型 | 接口 | 说明 |
|----------|------|------|
| **Protocol** | `IProtocolPlugin` | 新增通信协议 (Modbus, CANopen, EtherCAT) |
| **Device** | `IDevicePlugin` | 新增设备驱动 (Yaskawa, Siemens, Delta) |
| **TestStep** | `ITestStep` (via StepFactory) | 新增测试步骤类型 |
| **UI Panel** | `IDockWidget` | 新增 UI 面板 |
| **Data Filter** | `IDataFilter` | 新增数据处理滤波器 |
| **Export Format** | `IExportFormat` | 新增导出格式 (MATLAB, HDF5) |

### 10.2 插件清单 (plugin.json)

```json
{
    "id": "com.company.motor.protocol.modbus",
    "name": "Modbus RTU/TCP 通信插件",
    "version": "1.2.0",
    "type": "Protocol",
    "entryPoint": "createModbusPlugin",
    "minHostVersion": "1.0.0",
    "dependencies": [],
    "tags": ["modbus", "serial", "tcp"]
}
```

### 10.3 插件加载流程

```
1. 扫描 plugins/ 目录 → 发现 plugin.json
2. 验证版本兼容性 (minHostVersion ≤ hostVersion ≤ maxHostVersion)
3. 检查依赖 (dependencies)
4. 拓扑排序 → 确定加载顺序
5. 加载动态库 (dlopen/LoadLibrary)
6. 调用 entryPoint → 获取 IPlugin 实例
7. 调用 plugin->initialize()
8. 注册到 PluginRegistry
```

### 10.4 安全设计

| 机制 | 说明 |
|------|------|
| 数字签名验证 | 插件签名校验，防止篡改 |
| 沙箱隔离 | 可选独立进程，崩溃不影响主程序 |
| 权限声明 | 插件声明所需权限，用户审批 |
| 崩溃隔离 | 独立进程模式下自动重启 |

### 10.5 本章自我Review

**潜在问题：**

1. **问题**：不同插件可能使用不同版本的 C++ 标准库，ABI 不兼容。
   **改进**：使用 C ABI 作为插件边界（`extern "C"`），避免 C++ ABI 问题。

2. **问题**：插件依赖 `minHostVersion` 检查是字符串比较，对于复杂版本号（如 `1.0.0-beta.1`）可能不准确。
   **改进**：使用语义化版本 (SemVer) 库进行精确比较。

3. **问题**：插件卸载时可能留下悬空指针（其他模块持有插件对象的引用）。
   **改进**：插件卸载前发送 `aboutToUnload` 通知，给其他模块清理的机会。

4. **问题**：`plugin.json` 的 `entryPoint` 字段是字符串，加载时需要通过 `dlsym/GetProcAddress` 查找，缺乏类型安全。
   **改进**：使用统一的 `createPlugin` 和 `destroyPlugin` 函数名，减少出错可能。

5. **问题**：插件目录扫描是 O(N) 的磁盘操作，大量插件时启动变慢。
   **改进**：缓存已扫描的插件清单，仅在新插件出现时更新。

6. **问题**：插件之间可能存在隐式依赖（如 Device 插件依赖 Protocol 插件），但 `plugin.json` 中未声明。
   **改进**：强制要求 `dependencies` 中声明所有直接依赖，启动时校验。

---

## 第11章：日志系统

### 11.1 日志分类

| 分类 | 使用者 | 典型内容 | 存储策略 |
|------|--------|----------|----------|
| Debug | 开发者 | 变量值、函数调用栈 | 仅开发版本 |
| Info | 所有模块 | 连接成功、配置加载 | 保留 30 天 |
| Warning | 所有模块 | 超时重试、参数越界 | 保留 90 天 |
| Error | 所有模块 | 通信失败、CRC 校验失败 | 保留 180 天 |
| Critical | 核心模块 | 崩溃前兆、硬件异常 | 永久保留 |
| Protocol | Communication | 原始包 hex dump | 保留 7 天 |
| Automation | Automation | 测试用例开始/结束 | 按测试任务保留 |
| Device | DeviceManager | 设备连接/断开、固件版本 | 保留 90 天 |
| Curve | CurveEngine | 降采样点数、丢帧 | 保留 30 天 |
| Performance | 所有模块 | 函数耗时、吞吐量 | 保留 60 天 |

### 11.2 架构设计

```
调用层 (LOG_DEBUG / LOG_INFO / LOG_WARN / LOG_ERROR ...)
    │
    ▼
LogManager (单例)
    ├── Front Buffer (写入用) ──atomic swap──→ Back Buffer (消费用)
    └── Category Filter (运行时切换)
    │
    ├──→ ConsoleSink (ANSI颜色)
    ├──→ FileSink (按分类分文件，按日期/大小滚动)
    ├──→ NetworkSink (远程日志服务器)
    └──→ UISink (实时日志面板)
```

### 11.3 性能设计

| 策略 | 实现 | 收益 |
|------|------|------|
| 固定大小 Entry | 128B 对齐，无动态分配 | 零 malloc，cache 友好 |
| 无锁写入 | atomic + memory_order_relaxed | 写入 < 1μs |
| 批量刷盘 | 收集 4096 条或 10ms 后批量 write() | 减少系统调用 |
| 双缓冲 | atomic swap Front ↔ Back | 写入不阻塞 |

### 11.4 宏封装

```cpp
#define LOG_DEBUG(fmt, ...)    LogManager::instance().log(LogLevel::Debug, ...)
#define LOG_INFO(fmt, ...)     LogManager::instance().log(LogLevel::Info, ...)
#define LOG_WARN(fmt, ...)     LogManager::instance().log(LogLevel::Warning, ...)
#define LOG_ERROR(fmt, ...)    LogManager::instance().log(LogLevel::Error, ...)
#define LOG_CRITICAL(fmt, ...) LogManager::instance().log(LogLevel::Critical, ...)
#define LOG_PROTOCOL_TX(data, len) LogManager::instance().protocol(data, len, true)
#define LOG_PROTOCOL_RX(data, len) LogManager::instance().protocol(data, len, false)
#define LOG_PERF_SCOPE(name)  PerfTimer _perf_timer(name)
```

### 11.5 本章自我Review

**潜在问题：**

1. **问题**：`LogEntry` 固定 128B，`message[96]` 可能不足以容纳长消息。
   **改进**：长消息自动截断并添加 `...(truncated)` 标记，或使用外部存储指针。

2. **问题**：`FileSink` 按分类分文件，高频分类（如 Protocol）可能产生超大文件。
   **改进**：Protocol 日志按小时滚动，同时限制单文件最大 500MB。

3. **问题**：`NetworkSink` 使用 UDP 发送，可能丢失 Critical 级别日志。
   **改进**：Critical 级别使用 TCP 可靠传输，其他级别使用 UDP。

4. **问题**：`UISink` 通过 `QueuedConnection` 发送到 UI 线程，高负载下事件队列可能溢出。
   **改进**：UI 面板使用环形缓冲区 + 定时刷新（100ms），而非逐条推送。

5. **问题**：日志文件按日期滚动，但文件名不包含主机名，多设备环境可能冲突。
   **改进**：文件名格式 `{hostname}_{category}_{date}.log`。

6. **问题**：`LOG_PERF_SCOPE` 使用 RAII 计时器，但异常抛出时可能不记录。
   **改进**：在析构函数中使用 `std::uncaught_exceptions()` 检测异常状态。

---

## 第12章：线程模型

### 12.1 线程划分

| 线程 | 职责 | 创建时机 | 数量 |
|------|------|----------|------|
| **UI** | Qt 事件循环，UI 渲染 | 程序启动 (main) | 1 |
| **Communication** | 串口/USB/CAN/TCP 收发 | 用户连接设备时 | 1/连接 |
| **DataBus** | 数据解析、缓存、广播 | 程序启动 | 1 |
| **Curve** | 曲线数据处理、降采样 | 用户查看波形时 | 1/窗口 |
| **Logger** | 日志写入磁盘 | 程序启动最早 | 1 |
| **Automation** | 自动化测试执行 | 用户启动测试时 | 1 |
| **Script** | 脚本执行 | 脚本运行时 | 1/脚本 |
| **Recorder** | 数据记录写入磁盘 | 用户启动录制时 | 1 |
| **Update** | 固件升级 | 用户触发升级时 | 1 |

### 12.2 线程间通信

| 方式 | 适用场景 | 延迟 |
|------|----------|------|
| Qt 信号/槽 (QueuedConnection) | UI 更新、跨线程通知 | ~100μs |
| 无锁队列 (Lock-free Queue) | Comm → DataBus 数据流 | < 1μs |
| 环形缓冲区 (Ring Buffer) | 实时数据共享 | < 1μs |
| 共享内存 (只读) | 配置参数共享 | 0 (直接读) |

### 12.3 线程安全策略

| 数据类别 | 策略 | 示例 |
|----------|------|------|
| 线程独占数据 | 无需同步 | Comm 线程的串口缓冲区 |
| 不可变共享数据 | 无需同步 | 固件版本号 (只读) |
| SPSC 队列数据 | 无锁原子操作 | Comm → DataBus 数据流 |
| 读写少数据 | `std::shared_mutex` | 设备配置参数 |
| 高频读写数据 | 自旋锁 (spinlock) | 实时数据缓存 |
| UI 数据 | Qt QueuedConnection | 波形数据 → UI 更新 |

### 12.4 优雅关闭顺序

```
1. Automation 线程   ← 中止当前测试，保存结果
2. Script 线程       ← 发送终止信号，等待超时
3. Update 线程       ← 中止升级
4. Recorder 线程     ← 刷盘剩余数据
5. Curve 线程        ← 停止渲染
6. Comm 线程         ← 断开所有连接
7. DataBus 线程      ← 清空队列
8. Logger 线程       ← 刷盘所有日志 ← 最后销毁
9. UI 线程           ← 退出事件循环
```

### 12.5 设计理由

- **UI 线程不能做 IO**：Qt 事件循环是单线程的，IO 阻塞会导致 UI 冻结
- **通信线程必须独立**：串口/CAN 的 `readyRead` 信号需要及时响应，不能与 UI 共享线程
- **Logger 需要独立线程**：磁盘写入延迟不可控，不能阻塞任何业务线程
- **Curve 独立线程**：降采样计算可能耗时（大数据量），独立线程避免影响 UI 刷新

### 12.6 本章自我Review

**潜在问题：**

1. **问题**：10 个线程在低端硬件上可能导致上下文切换开销过大。
   **改进**：大部分线程按需创建，空闲时自动休眠或销毁，实际运行线程数通常 < 5。

2. **问题**：`Communication` 线程使用 `QSerialPort` 的 `readyRead` 信号，但 `QSerialPort` 的信号在哪个线程触发取决于 `moveToThread`。
   **改进**：明确 `QSerialPort` 对象所属线程，确保信号在正确的线程上下文中处理。

3. **问题**：`DataBus` 线程作为中心节点，如果该线程卡死，整个系统的数据流中断。
   **改进**：增加 DataBus 线程的 Watchdog，5s 无响应则触发告警和自动恢复。

4. **问题**：`Curve` 线程按窗口创建，多窗口时可能产生大量线程。
   **改进**：限制最大 Curve 线程数为 4，超出时共享线程池。

5. **问题**：`Recorder` 线程写入磁盘时可能因磁盘满而阻塞，但没有明确的错误处理路径。
   **改进**：`Recorder` 在磁盘空间低于阈值时主动暂停录制并通知用户。

6. **问题**：线程生命周期管理依赖手动控制，容易遗漏清理。
   **改进**：使用 RAII 封装线程生命周期，`ThreadGuard` 在析构时自动 join。

---

## 第13章：三年 RoadMap

### 13.1 总体路线

```
2026 Q3-Q4          2027 Q1-Q2          2027 Q3-Q4          2028 Q1-Q2          2028 Q3-Q4
│ 第一阶段 MVP       │ 第二阶段 增强       │ 第二阶段 增强       │ 第三阶段 成熟       │ 第三阶段 成熟
│                    │                    │                    │                    │
│ 核心框架           │ 扩展协议           │ 自动化测试         │ 插件生态           │ 高级功能
│ 通信协议           │ 多设备支持         │ 脚本引擎           │ 社区贡献           │ AI 辅助
│ 参数配置           │ 数据记录           │ 固件升级           │ 跨平台             │ 云端集成
│ 实时波形           │ 高级调试           │ 报告生成           │ 开放 API           │ 产品化
│ 日志系统           │                    │                    │                    │
│ 线程模型           │                    │                    │                    │
```

### 13.2 第一阶段：MVP (0-6个月，2026 Q3-Q4)

**目标**：可用的最小产品——能连接设备、配置参数、查看实时波形、记录日志。

| 优先级 | 模块 | 功能 | 预估工时 |
|--------|------|------|----------|
| P0 | 基础设施 | CMake 工程结构、编码规范、Git 工作流 | 1 周 |
| P0 | 日志系统 | 10 分类 + 4 Sink + 双缓冲 + 异步 | 2 周 |
| P0 | 线程模型 | 10 线程架构 + 无锁队列 + 线程安全 | 2 周 |
| P0 | 通信协议 | 二进制包协议 (CRC16 + 状态机解包) | 3 周 |
| P0 | 串口通信 | 串口扫描、连接、收发、超时重试 | 2 周 |
| P0 | 连接管理 | 设备发现、连接/断开、状态指示 | 1 周 |
| P0 | 参数配置 | 参数模型、配置页框架、读写配置 | 3 周 |
| P1 | 实时数据 | DataBus 实现、环形缓冲区 | 2 周 |
| P1 | 实时波形 | QGraphicsView 集成、多曲线显示 | 2 周 |
| P1 | 数值仪表盘 | 电压/电流/转速/温度数值显示 | 1 周 |
| P1 | 配置导入导出 | JSON 配置文件读写 | 1 周 |
| P2 | 键盘控制 | 方向键控制电机 | 0.5 周 |
| P2 | 故障显示 | 故障码解析、颜色指示 | 1 周 |

**里程碑**：
- M1.1 (第4周)：基础框架就绪，编译运行
- M1.2 (第9周)：通信就绪，可连接设备
- M1.3 (第14周)：配置可用，可读写参数
- M1.4 (第18周)：波形可用，实时数据显示
- M1.5 (第22周)：MVP 发布，内部测试

### 13.3 第二阶段：增强 (6-18个月，2027 Q1-Q4)

**目标**：完善的工业级工具——支持多协议、多设备、数据记录、自动化测试、固件升级。

| 模块 | 功能 | 预估工时 |
|------|------|----------|
| CAN 通信 | CAN Bus 协议支持 | 4 周 |
| TCP/UDP 通信 | WiFi/以太网远程连接 | 3 周 |
| 多设备管理 | 同时连接多个设备 | 3 周 |
| 数据记录 | 实时数据记录到 CSV/二进制 | 3 周 |
| 数据回放 | 加载历史数据、波形回放 | 2 周 |
| 固件升级 | 分块上传、CRC 校验、失败回滚 | 3 周 |
| 自动检测 | 电机参数自动检测 (R/L/磁链) | 3 周 |
| 在线调参 | 实时修改 PID/限值参数 | 2 周 |
| 自动化测试 | 测试用例编排、断言检查 | 4 周 |
| 脚本引擎 | Lua 脚本嵌入、API 暴露 | 4 周 |
| 报告生成 | 测试报告 (PDF/HTML) | 2 周 |

**里程碑**：
- M2.1 (第7月)：CAN + TCP 通信可用
- M2.2 (第9月)：数据记录 + 回放可用
- M2.3 (第11月)：固件升级可用
- M2.4 (第13月)：自动检测 + 在线调参可用
- M2.5 (第16月)：自动化测试 + 脚本引擎可用
- M2.6 (第18月)：v2.0 正式发布

### 13.4 第三阶段：成熟 (18-36个月，2028 Q1-Q4)

**目标**：平台化产品——插件生态、跨平台、AI 辅助、云端集成。

| 模块 | 功能 |
|------|------|
| 插件系统 | 动态加载 .dll/.so 插件、插件市场、版本管理 |
| 跨平台 | macOS 支持、Linux 完整支持 |
| Python 脚本 | 完整 Python API (pybind11)、Jupyter 集成 |
| AI 辅助调参 | 基于历史数据的 PID 推荐、异常检测 |
| 云端集成 | 远程日志上传、设备管理云平台、OTA 固件升级 |
| 开放 API | REST API、WebSocket 实时数据流、gRPC 接口 |
| 自定义仪表盘 | 拖拽式仪表盘编辑器、自定义布局、主题 |
| 多语言 | i18n 框架、中/英/日/德 语言包 |
| 安全认证 | 用户权限管理、操作审计、安全日志 |

### 13.5 风险分析

| 风险 | 严重度 | 可能性 | 缓解措施 |
|------|--------|--------|----------|
| MCU 固件协议不兼容 | 高 | 中 | 第一阶段与固件团队紧密协作，定义稳定协议版本号 |
| QGraphicsView 性能瓶颈 | 中 | 低 | 提前做性能基准测试，准备 OpenGL 备选方案 |
| 多线程死锁/竞态 | 高 | 中 | 严格代码审查、ThreadSanitizer 检测、Watchdog 监控 |
| 串口驱动兼容性 | 中 | 高 | 支持多种串口库 (QSerialPort + Windows API 回退) |
| 第三方库 License | 低 | 中 | 第一阶段即明确依赖清单和 License 审查 |
| 人力不足 | 高 | 中 | 第一阶段优先核心功能，非核心功能可延后 |

### 13.6 设计原则

**基础设施优先**：
```
日志系统 → 线程模型 → 通信协议 → 参数配置 → 实时数据 → UI
```

原因：
- 没有日志，无法调试任何问题
- 没有线程模型，性能无法保证
- 没有通信协议，一切无从谈起
- UI 是最后一步：在数据流打通之前，UI 只是空壳

---

## 附录A：全局架构 Review

### A.1 跨模块一致性检查

| 检查项 | 状态 | 说明 |
|--------|------|------|
| 命名规范统一 | ✅ | 所有模块使用 PascalCase，接口使用 I 前缀 |
| 单例模式一致 | ✅ | 核心模块使用单例，数据处理模块使用多实例 |
| 异步模式一致 | ✅ | 所有 IO 操作使用 QFuture/Promise 异步模式 |
| 错误处理一致 | ✅ | 统一使用 Result<T> 或 optional<T> 返回错误 |
| 线程模型一致 | ✅ | 所有模块遵循线程划分，不跨线程直接访问 |
| 接口隔离一致 | ✅ | 所有模块通过抽象接口交互，不直接依赖实现 |

### A.2 潜在架构冲突及解决

| 冲突 | 涉及模块 | 解决方案 |
|------|----------|----------|
| DataBus 作为单点瓶颈 | DataBus, Curve, Logger, Recorder | 使用无锁 RingBuffer + 批量发布，目标 < 1μs/publish |
| Plugin 加载顺序依赖 | Plugin, Communication, Automation | 拓扑排序加载，循环依赖检测 |
| 参数缓存一致性 | ParameterManager, DataBus, Protocol | 参数变更通过 DataBus 广播，缓存自动失效 |
| 脚本引擎与自动化引擎功能重叠 | ScriptEngine, Automation | Automation 是高层编排，ScriptEngine 是底层执行 |
| 日志系统 vs DataBus 数据流 | Logger, DataBus | Logger 独立于 DataBus，使用专用双缓冲通道 |

### A.3 深度交叉审查（逐章对比）

以下是对全部13个章节进行交叉对比后发现的 **10 个关键问题**，以及对应的解决方案：

#### 问题 1：Protocol 层与 ParameterManager 的地址映射职责不清

**冲突描述**：
- 第3章（模块划分）中，`Protocol` 负责"帧解析、校验、分包/组包"
- 第4章（通讯架构）中，帧结构包含 `CMD` 字段，暗示协议层需要理解命令语义
- 第7章（参数系统）中，参数使用 `address` 字段，但 `address` 到 `CMD` 的映射由谁负责？

**根本原因**：Protocol 层被设计为"薄协议层"（只做帧解析），但实际需要理解命令语义才能正确路由响应。

**解决方案**：
```
新增一个 CommandMapper 层（位于 Protocol 之上，ParameterManager 之下）：
- 负责 address → CMD 映射
- 负责参数类型 → 编解码格式映射
- 负责 MotorLink 协议版本兼容性

Protocol 层保持纯粹：只负责帧边界检测、CRC、COBS 编解码
```

#### 问题 2：Communication 线程在"多设备连接"场景下的线程模型冲突

**冲突描述**：
- 第12章（线程模型）中，Communication 线程为 `1/连接`
- 第4章（通讯架构）中，`CommunicationManager` 是单例
- 第3章（模块划分）中，`DeviceManager` 管理多设备

如果同时连接 3 个设备（如 1 个串口 + 1 个 CAN + 1 个 TCP），需要 3 个 Communication 线程，但 `CommunicationManager` 是单例——它如何管理 3 个线程？

**解决方案**：
```
CommunicationManager 内部使用 TransportManager：
- 每个 ITransport 实例运行在独立线程
- TransportManager 维护 Transport → Thread 映射
- 线程按需创建（连接时），断开时销毁（延迟销毁，避免频繁创建）
- CommunicationManager 本身是线程安全的，不绑定特定线程
```

#### 问题 3：DataBus 的 publish() 回调中调用其他模块导致死锁风险

**冲突描述**：
- 第5章（数据总线）中，`publish()` 在 DataBus 线程中调用订阅回调
- 如果 CurvEngine 的回调中又调用了 `DataBus::subscribe()`（例如动态添加通道）
- 或者 Logger 的回调中调用了 `ParameterManager::readParam()`（需要通信）
- 都可能触发死锁

**根本原因**：Pub/Sub 模式中，回调在 publish 调用栈中同步执行，回调中的操作可能重新进入 DataBus。

**解决方案**：
```
1. DataBus 的回调分发使用异步模式（Qt::QueuedConnection）
2. publish() 将回调放入队列后立即返回，不等待回调执行
3. 订阅者不能假设回调执行时数据仍然"最新"——应使用 snapshot() 获取
4. 禁止在回调中调用 publish()（编译期检查或运行时断言）
```

#### 问题 4：CurveEngine 和 Recorder 的数据流竞争

**冲突描述**：
- 第5章（数据总线）中，DataBus 同时向 CurveEngine 和 Recorder 发布数据
- 第6章（波形系统）中，CurveEngine 使用 RingBuffer 缓存历史数据
- Recorder 也需要写磁盘

如果二者都从 DataBus 的 RingBuffer 读取，谁先读？先读的会消费数据，后读的可能丢失。

**解决方案**：
```
DataBus 使用"广播语义"而非"队列消费语义"：
- publish() 将数据写入多个独立的 RingBuffer（每个订阅者一个）
- 或者使用引用计数共享数据（DataPoint 不可变，多消费者共享）
- 订阅者各自维护读取指针，互不影响
```

#### 问题 5：脚本系统与自动化引擎的线程安全

**冲突描述**：
- 第9章（脚本系统）中，脚本在后台线程执行
- 第8章（自动化测试）中，`TestContext` 在步骤间共享状态
- 如果脚本调用 `motor.setTargetSpeed()`，这会触发 Communication 线程的异步操作
- 脚本线程需要等待结果，但 Communication 线程的结果通过 DataBus 异步返回

**根本原因**：脚本是同步语义（`setSpeed()` 后等待结果），但底层是异步架构。

**解决方案**：
```
1. 脚本 API 使用协程语义（Lua coroutine / Python async/await）
2. motor.setTargetSpeed() 在脚本中返回 Promise
3. 脚本引擎自动 await Promise，协程挂起
4. 结果到达时恢复协程执行
5. 对用户透明：脚本看起来是同步的，实际是异步的
```

#### 问题 6：Plugin 动态库的 C++ ABI 兼容性

**冲突描述**：
- 第10章（插件系统）中提到使用 `extern "C"` 作为插件边界
- 但插件接口 `IProtocolPlugin`、`IDevicePlugin` 使用 C++ 虚函数
- 如果主程序和插件使用不同编译器（MSVC vs MinGW）或不同版本的 C++ 标准库编译，虚函数表布局可能不兼容

**解决方案**：
```
使用纯 C ABI 作为插件边界：
1. 插件导出 C 函数：createPlugin() → 返回不透明句柄
2. 主程序通过 C 函数调用插件功能，传入函数指针表
3. 插件内部可以使用 C++ 实现，但边界必须是 C
4. 使用 PIMPL 模式隐藏 C++ 实现细节

示例：
// 插件导出
extern "C" {
    IPlugin* createPlugin(IPluginHost* host, const PluginConfig* config);
    void destroyPlugin(IPlugin* plugin);
    uint32_t getPluginAPIVersion();
}

// IPluginHost 使用纯虚接口（C ABI 兼容层）
// 插件和主程序各自编译，通过虚函数表通信（需确保 ABI 兼容）
```

#### 问题 7：Logger 和 DataBus 对同一数据的重复记录

**冲突描述**：
- 第11章（日志系统）中，Logger 有独立的 `Curve` 分类日志
- 第5章（数据总线）中，DataBus 向 Logger 发布数据
- 第6章（波形系统）中，CurveEngine 也有自己的数据缓存

如果 Logger 记录了所有 DataBus 数据（Curve 分类），同时 Recorder 也记录了数据（Recorder 分类），会产生大量重复数据和时间戳不一致问题。

**解决方案**：
```
明确职责边界：
- Logger：记录"元数据"而非"数据本身"
  - 例如：Curve 分类记录"降采样从 100万点 → 1000点，耗时 2.3ms"
  - 而非"DataPoint(t=1234, Iq=5.6)"  ← 这是 Recorder 的职责
- Recorder：记录原始数据点到磁盘
  - 使用高效二进制格式（如 HDF5 或自定义格式）
  - 支持按时间范围查询
- DataBus：内存中的实时数据缓存
  - 不持久化，只提供环形缓冲区
```

#### 问题 8：固件升级（Update）与正常通信的资源竞争

**冲突描述**：
- 第4章（通讯架构）中，Command 和 Stream 共用同一 Transport
- 第12章（线程模型）中，Update 有独立线程
- 固件升级时，需要独占 Transport（大量数据传输），但实时数据流仍在推送

固件升级期间，Stream 数据会与升级数据竞争带宽，导致升级变慢或失败。

**解决方案**：
```
固件升级时切换到"升级模式"：
1. Update 线程向 CommunicationManager 请求"独占模式"
2. CommunicationManager 暂停 Stream 数据流（发送 PAUSE_STREAM 命令到 MCU）
3. MCU 停止推送实时数据，进入升级模式
4. 升级完成后，恢复 Stream 数据流
5. 升级失败时，自动恢复 Stream（Watchdog 机制）

注意：这不是"同步等待"，而是"模式切换"
- 模式切换本身是异步的
- 升级数据使用独立的 Command 优先级
```

#### 问题 9：参数系统的"自动生成UI"与插件系统的扩展冲突

**冲突描述**：
- 第7章（参数系统）中，参数描述文件定义了控件类型和布局
- 第10章（插件系统）中，插件可以添加新的 UI 面板
- 如果插件添加了自定义参数类型（如 `ComplexNumber`），UI 自动生成引擎无法识别

**解决方案**：
```
参数系统使用"控件工厂"模式：
1. 每种参数类型注册一个 WidgetFactory
2. 插件可以注册新的 WidgetFactory
3. UI 自动生成时，查找对应的 WidgetFactory 创建控件
4. 默认 WidgetFactory 处理内置类型（Bool/Int/Float/Enum/Bit/String）
5. 插件 WidgetFactory 处理自定义类型

// 内置注册
WidgetFactoryRegistry::registerFactory("Float32", &Float32Widget::create);
WidgetFactoryRegistry::registerFactory("Enum", &EnumComboBox::create);

// 插件注册
// 在 plugin.json 中声明
{
    "widgetFactories": [
        { "type": "ComplexNumber", "factory": "createComplexWidget" }
    ]
}
```

#### 问题 10：RoadMap 中"基础设施优先"原则与快速迭代需求的矛盾

**冲突描述**：
- 第13章（RoadMap）中，第一阶段前 5 周都在做基础设施（CMake、日志、线程模型）
- 管理层可能要求在 2 周内看到"能连接设备"
- 日志系统、线程模型这些"看不见"的工作难以展示进度

**解决方案**：
```
采用"纵向切片"策略：
1. 第一阶段第一周不做"完整的日志系统"，而是做"最小可用日志"
   - 只实现 ConsoleSink + FileSink
   - 只实现 Debug/Info/Warn/Error 四个级别
   - 日志系统框架预留扩展点，但只实现核心路径

2. 第一阶段第一周不做"完整的线程模型"，而是做"最小可用线程"
   - UI 线程 + Communication 线程（2个线程）
   - 其他线程在需要时添加

3. 第2周就能 demo：连接设备 + 读取参数 + 显示波形
   - 这个 demo 使用的是"简化版"基础设施
   - 后续迭代中逐步完善

4. 每个里程碑都产出可运行的 demo
   - 而非"前5周只做基础设施，第6周才看到东西"
```

---

### A.4 全局架构全景图

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                            motor_antomation 全局架构                              │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                         UI Layer (ui/)                               │   │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐  │   │
│  │  │Dashboard │ │  Curve   │ │ Parameter│ │Automation│ │  Script  │  │   │
│  │  │  Panel   │ │  Widget  │ │  Editor  │ │  Editor  │ │  Editor  │  │   │
│  │  └────┬─────┘ └────┬─────┘ └────┬─────┘ └────┬─────┘ └────┬─────┘  │   │
│  └───────┼────────────┼────────────┼────────────┼────────────┼────────┘   │
│          │            │            │            │            │            │
│  ┌───────┼────────────┼────────────┼────────────┼────────────┼────────┐   │
│  │       │       Service Layer (services/)      │            │        │   │
│  │  ┌────┴─────┐ ┌────┴─────┐ ┌────┴─────┐ ┌────┴─────┐ ┌────┴─────┐  │   │
│  │  │  Curve   │ │Parameter │ │Automation│ │  Script  │ │ Project  │  │   │
│  │  │ Engine   │ │ Manager  │ │  Engine  │ │  Engine  │ │ Manager  │  │   │
│  │  └────┬─────┘ └────┬─────┘ └────┬─────┘ └────┬─────┘ └────┬─────┘  │   │
│  └───────┼────────────┼────────────┼────────────┼────────────┼────────┘   │
│          │            │            │            │            │            │
│  ┌───────┴────────────┴────────────┴────────────┴────────────┴────────┐   │
│  │                        Core Layer (core/)                           │   │
│  │  ┌──────────────────────────────────────────────────────────────┐  │   │
│  │  │                       DataBus (Pub/Sub)                       │  │   │
│  │  │  RingBuffer[] ← 无锁 ← Transport Thread → 批量发布            │  │   │
│  │  └──────────────────────────┬───────────────────────────────────┘  │   │
│  │                             │                                      │   │
│  │  ┌──────────────────────────┼───────────────────────────────────┐  │   │
│  │  │                    DeviceManager                              │  │   │
│  │  │  设备发现 | 连接管理 | 心跳 | 状态监控 | 多设备协调              │  │   │
│  │  └──────────────────────────┬───────────────────────────────────┘  │   │
│  │                             │                                      │   │
│  │  ┌──────────┐ ┌─────────────┴──────────┐ ┌──────────┐            │   │
│  │  │ Recorder │ │   CommandMapper        │ │  Update  │            │   │
│  │  │ (磁盘IO) │ │ (address→CMD映射)      │ │ (固件升级)│            │   │
│  │  └──────────┘ └─────────────┬──────────┘ └──────────┘            │   │
│  └─────────────────────────────┼─────────────────────────────────────┘   │
│                                │                                         │
│  ┌─────────────────────────────┼─────────────────────────────────────┐   │
│  │                     Protocol Layer (protocol/)                     │   │
│  │  ┌──────────────────────────┴──────────────────────────────────┐  │   │
│  │  │  FrameCodec (COBS → 帧边界 → CRC16 → 命令/流数据分流)        │  │   │
│  │  └──────────────────────────┬──────────────────────────────────┘  │   │
│  └─────────────────────────────┼─────────────────────────────────────┘   │
│                                │                                         │
│  ┌─────────────────────────────┼─────────────────────────────────────┐   │
│  │                Communication Layer (communication/)               │   │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌────────┐ │   │
│  │  │  Serial  │ │ USB CDC  │ │   CAN    │ │   TCP    │ │  UDP   │ │   │
│  │  │Transport │ │Transport │ │Transport │ │Transport │ │Transport│ │   │
│  │  └──────────┘ └──────────┘ └──────────┘ └──────────┘ └────────┘ │   │
│  └───────────────────────────────────────────────────────────────────┘   │
│                                                                         │
│  ┌───────────────────────────────────────────────────────────────────┐   │
│  │                    Infrastructure Layer                            │   │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐            │   │
│  │  │  Logger  │ │ Settings │ │  Plugin  │ │   HAL    │            │   │
│  │  │ (双缓冲)  │ │ (JSON)   │ │ (动态库)  │ │ (平台抽象)│            │   │
│  │  └──────────┘ └──────────┘ └──────────┘ └──────────┘            │   │
│  └───────────────────────────────────────────────────────────────────┘   │
│                                                                         │
│  ┌───────────────────────────────────────────────────────────────────┐   │
│  │                   Thread Model (线程模型)                          │   │
│  │                                                                   │   │
│  │  UI Thread ──── QueuedConnection ──── All Service Threads         │   │
│  │  Comm Thread(s) ──── Lock-free Queue ──── DataBus Thread          │   │
│  │  Logger Thread ──── Double Buffer ──── Disk Write                 │   │
│  │  Curve Thread ──── RingBuffer ──── GPU Render                     │   │
│  │  Automation Thread ──── Command Queue ──── Comm Thread            │   │
│  │  Script Thread ──── Coroutine ──── Async API                      │   │
│  │  Recorder Thread ──── RingBuffer ──── Disk Write                  │   │
│  │  Update Thread ──── Exclusive Mode ──── Comm Thread               │   │
│  └───────────────────────────────────────────────────────────────────┘   │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### A.5 关键设计决策汇总

以下是贯穿整个架构的 **10 个核心设计决策**，作为后续开发的"宪法"：

| # | 决策 | 理由 | 不可违背 |
|---|------|------|----------|
| 1 | **所有 IO 操作异步** | UI 线程不能阻塞 | ✅ 红线 |
| 2 | **DataBus 为唯一数据通道** | 各模块不直接访问 Transport | ✅ 红线 |
| 3 | **Protocol 层只做帧解析** | 职责单一，CommandMapper 负责语义映射 | ✅ 红线 |
| 4 | **插件使用 C ABI 边界** | 避免 C++ ABI 兼容性问题 | ✅ 红线 |
| 5 | **Pub/Sub 回调异步分发** | 防止死锁和回调重入 | ✅ 红线 |
| 6 | **DataBus 广播语义，非消费语义** | 多消费者独立读取，不互相影响 | ✅ 红线 |
| 7 | **固件升级使用独占模式** | 避免与实时数据流竞争带宽 | ✅ 红线 |
| 8 | **Logger 记录元数据，Recorder 记录数据** | 职责分离，避免重复存储 | ⚠️ 强建议 |
| 9 | **参数系统使用逻辑ID为主键** | 地址是传输映射，固件版本变化时只改映射 | ⚠️ 强建议 |
| 10 | **纵向切片，每阶段产出可运行 demo** | 管理层可视化进度，降低风险 | ⚠️ 强建议 |

### A.6 最终架构评审结论

**优点**：
1. ✅ 分层清晰：Hal → Core/Protocol → Service → UI，依赖单向
2. ✅ 完全异步：基于 Promise/Future + 信号/槽，零同步等待
3. ✅ 高度解耦：DataBus 作为中心数据总线，Pub/Sub 模式，广播语义
4. ✅ 可扩展：插件系统 + 工厂模式 + 策略模式 + 控件工厂
5. ✅ 工业级性能：无锁 RingBuffer、双缓冲日志、LTTB 降采样、零拷贝
6. ✅ 线程安全：明确线程划分、锁策略、死锁预防、优雅关闭
7. ✅ 职责清晰：每个模块职责单一，边界明确
8. ✅ 技术栈合理：Qt6 + C++20 + CMake，跨平台，生态成熟

**待改进（按优先级排序）**：
1. 🔴 **必须**：增加 CommandMapper 层，解决 Protocol 与 ParameterManager 的地址映射问题
2. 🔴 **必须**：明确 CommunicationManager 的多 Transport 管理模式
3. 🔴 **必须**：DataBus 回调改为异步分发，防止死锁
4. 🟡 **重要**：DataBus 实现广播语义（多 RingBuffer），而非消费语义
5. 🟡 **重要**：脚本 API 使用协程语义，适配底层异步架构
6. 🟡 **重要**：插件边界明确 C ABI 标准，防止编译器兼容性问题
7. 🟢 **建议**：参数系统增加控件工厂，支持插件自定义参数类型
8. 🟢 **建议**：固件升级增加独占模式，避免带宽竞争
9. 🟢 **建议**：RoadMap 采用纵向切片，每阶段产出可运行 demo
10. 🟢 **建议**：增加集成测试、性能基准测试、崩溃报告的自动化覆盖

**总体评价**：
本架构设计达到了工业级电机调试平台的架构要求。核心设计原则（异步、解耦、分层、可扩展）贯穿始终。通过本轮的深度交叉审查，发现了 10 个关键架构问题并给出了解决方案。后续开发应严格遵循本设计文档，在详细设计阶段对每个模块进行进一步的接口定义和实现规划。

---

> **文档结束**  
> **版本**: v1.3 (产品需求访谈完成)  
> **编制日期**: 2026-08-04（初版），2026-08-05（全部六轮需求访谈完成）  
> **项目**: 工业级电机调试与自动化测试平台 (Motor Automation)  
> **技术栈**: Qt6 / C++20 / CMake  
> **目标平台**: Windows（主力）/ Linux（后续）  
>  
> 本文档为 Motor Automation 项目的总体软件设计文档，用于指导后续开发工作。  
> 各模块的详细设计将在对应的详细设计文档中展开。