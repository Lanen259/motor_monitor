# 波形域（domain/waveform）夜间防卡死自测任务书

> 文档版本：v1.0 ｜ 编制日期：2026-08-09 ｜ 编制角色：集成代理代拟，执行者 = 波形域执行 agent
> 执行方式：**通宵全自动**。本文件交给一个独立 agent 线程，在 `E:\My_project\QT\Motor_Antomation-wt\waveform` 内循环执行「审计 → 复现 → 修复 → 压力测试 → 门禁验证」，**直到 §11 验收矩阵全绿为止**。除 C 盘文件操作外不需要人工介入。
> 共享规格：通用机制（卡死判定、看门狗、报告格式、合并规则）见 master 分支 `docs/夜间防卡死自测_共享执行规格.md`（本 worktree 尚无时，先执行 Phase 0 的 `git merge master` 拉入）。

---

## 0. 任务目标与零容忍标准

1. **零卡死**：波形域所有可视化控件，在任何用户交互与任何数据流速下不得出现界面冻结 / 无响应 / 假死（判定：UI 线程事件循环单次阻塞 > 300ms，见共享规格 §2）。
2. **零崩溃**：不得出现段错误、堆损坏、未捕获异常导致的进程退出。
3. **禁止打补丁式修复**：先建立全局缺陷清单，**同一类根因用同一方案一次修完**，每类根因配回归测试。
4. 覆盖范围（CLAUDE.md §2）：`src/curve/**`、`src/ui/CurveWidget`、`CurveManagerPanel`、`MultiCurveContainer`、`PlotCell`、`VerticalPlotList`、`HistoryReplayWidget`、`ChannelConfigDialog`。

---

## 1. 本域特有高危场景（审计重点）

### 1.1 用户当前反馈的通用症状（全域共性，本域同样必查）

| 编号 | 操作序列 | 现象 |
|---|---|---|
| SYM-1 | 在任意输入控件中输入数据 → **立刻按 Enter** → **100ms 内拖动控件** | 界面卡死 |
| SYM-2 | 输入后不按 Enter 直接拖拽 / 切换焦点 | 偶发卡死 |

### 1.2 波形域专属高危模式

| 模式 | 说明 | 高发位置 |
|---|---|---|
| W1 高频重绘风暴 | 数据帧率高于渲染帧率时每个数据点触发 repaint / 未做按帧合并 | `CurveWidget`（pushData/pushFrame 直推模式）、`PlotCell` |
| W2 定时器竞争 | `attachCurveEngine` 拉取定时器与数据推送并发，detach/重建时定时器悬空 | `CurveWidget::attachCurveEngine/detachCurveEngine` |
| W3 拖拽缩放重入 | 橡皮筋缩放（rubber-band，WI-104/105）拖动期间触发通道重绑定/重布局；Y 轴偏移平移（WI-801）与滚动拖拽互相抢占鼠标事件 | `CurveWidget` 鼠标事件链 |
| W4 容器增删与渲染并发 | 增删 PlotCell / 通道时渲染线程或定时器仍在引用旧对象 | `MultiCurveContainer`、`VerticalPlotList`、`ChannelConfigDialog` 确认路径 |
| W5 回放拖动事件风暴 | 历史回放滑条拖动时每个 move 事件触发全量解码/重绘 | `HistoryReplayWidget` |
| W6 通道配置对话框重入 | 对话框确认时应用配置 → 触发曲线重建 → 又触发配置变更信号 | `ChannelConfigDialog`、`CurveManagerPanel` |

---

## 2. 执行规则（强制）

1. **四阶段顺序不可跳过**：Phase 1 审计 → Phase 2 复现用例 → Phase 3 统一修复 → Phase 4 仿真压力测试。每 Phase 结束输出阶段报告再继续。
2. **只改本域名下文件**（§0.4）。根因落在 master-owned 红线文件（`mainwindow.*`、`src/core/**`、`src/databus/**` 等）时**不得修改**，报告中标注「需集成代理处理」。
3. **证据优先**：一切"已修复/已验证"必须附命令输出（CLAUDE.md §10）。
4. **每处修复配套回归测试**：修复前红、修复后绿，否则视为无效。
5. 提交规范：前缀 `[波形]`，写清根因模式编号（W1-W6 / P1-P6，P 编号见共享规格 §3）。

---

## 3. Phase 0 — 环境准备与基线

1. `cd E:\My_project\QT\Motor_Antomation-wt\waveform`，`git status`；本任务之外的未提交改动先按归属提交或 stash。
2. **同步 master**：`git merge master`（master 已有门禁基础设施与测试修复提交，域分支落后必须先合并）。冲突只在 master-owned 文件 → 上报，不自行改写。
3. 构建 + 全量测试建立基线：
   ```
   scripts\gate.bat     （本 worktree 无此文件时用主 checkout：E:\My_project\QT\Motor_Antomation\scripts\gate.bat，工作目录设为本 worktree 的 motor_antomation\）
   ctest --test-dir build\ci-tests --output-on-failure
   ```
4. 基线写入 `reports/nightly_baseline_waveform.md`。
5. UI 测试一律 `QT_QPA_PLATFORM=offscreen`；数据一律走 `DeviceSimulator → VofaParser → DataBus → CurveEngine` 模拟管线（已有 `tests/integration/test_mock_mcu_curve.cpp` 可参考）。

**Phase 0 验收**：基线报告产出，gate.bat 结果明确。

---

## 4. Phase 1 — 只读审计（不改代码）

交互矩阵（每个控件都要过）：

| 控件 | 必须审计的交互 |
|---|---|
| `CurveWidget` | 高速 pushData/pushFrame（模拟 1kHz+）、rubber-band 缩放拖动、Y 偏移平移、滚动拖拽、两种模式切换（直推 ↔ engine 拉取）、通道动态增删时持续供数 |
| `MultiCurveContainer` / `PlotCell` | 增删 PlotCell、PlotCell 内通道重绑定、容器 resize 与供数并发 |
| `VerticalPlotList` | 列表滚动、项增删、选中切换 |
| `CurveManagerPanel` | 面板操作与实时渲染并发 |
| `ChannelConfigDialog` | 打开→编辑→确认→取消 各路径，确认后立刻拖动曲线区 |
| `HistoryReplayWidget` | 滑条高速拖动、播放/暂停/倍速切换、回放中切换实时模式 |

审计输出：缺陷清单表【编号｜控件｜根因模式(W1-W6/P1-P6)｜文件:行号｜触发序列｜风险等级】，SYM-1/2 在本域的实例必须定位到具体代码路径。

**Phase 1 验收**：清单入库 `reports/audit_waveform.md`。

---

## 5. Phase 2 — QTest 复现用例（修复前必须红）

1. 为清单每项写 QTest 用例：`motor_antomation/tests/unit/ui/test_<控件名>_interaction.cpp`（目录不存在则创建）。
2. 高频场景用 `DeviceSimulator` 供数 + 定时器驱动，不依赖真实串口；交互用 `QTest::keyClicks / mousePress / mouseMove / mouseRelease` 精确复刻，SYM-1 必须含「键入 → Enter → 立即拖动」时序。
3. 全部 `add_test()` 注册（D3）；卡死类用例带看门狗（共享规格 §2），`set_tests_properties(... TIMEOUT 60)` 防挂起。
4. 确认修复前全部失败/超时（红）。

**Phase 2 验收**：`ctest -N` 列全新用例，修复前红有证据。

---

## 6. Phase 3 — 统一修复

1. 同根因模式一次修完（如 W1 统一做"渲染按帧合并 + 脏标记重绘"，而不是每个控件各加节流）。
2. 优先公共层设防：渲染节流、定时器生命周期规约（attach/detach 对称、销毁前必停表）、容器增删锁/快照规约，写进域内公共工具。
3. P4/W1/W5 类事件风暴修复必须用 Phase 4 高频压力证明。
4. 每修一组立即 `gate.bat` + `ctest`，先绿再提交。
5. 红线文件问题只记录、不修改。

**Phase 3 验收**：清单全部闭环或标注移交；每个修复提交附测试由红转绿证据。

---

## 7. Phase 4 — Debug 模拟仿真压力测试（本夜核心环节）

1. 新建 `tests/stress/stress_waveform_ui.cpp`（`add_test()`，TIMEOUT 600）：
   - 构造 `MultiCurveContainer` + `CurveEngine` + `DeviceSimulator`，挂载 ≥8 通道。
   - **高频数据 + 随机交互叠加**：模拟管线以 ≥1kHz 帧率持续供数；同时用固定种子伪随机序列从操作池抽取并连续执行 ≥500 次：增删通道/PlotCell、rubber-band 缩放拖动、Y 偏移平移、滚动拖拽、打开/确认通道配置对话框、回放滑条拖动、播放暂停切换、直推/拉取模式切换。每步间 0~5ms 随机延迟。
   - 全程挂事件循环延迟看门狗（共享规格 §2）：单次事件处理 >300ms 即 `QFAIL`，输出当时操作序列与帧率。
   - 结束断言：无崩溃、看门狗零触发、渲染帧率统计正常（不因内存/对象泄漏衰减）、通道数自检一致。
2. **3 轮不同种子**全绿才算通过。
3. 压力测试暴露新问题 → 回 Phase 1 补清单 → 重走 2/3 → 再跑 4。允许且预期循环。

**Phase 4 验收**：3 轮全绿、看门狗零触发、ctest 输出入报告。

---

## 8. 通宵循环协议（agent 自主运转规则）

1. 循环体：发现问题 → 定位 → 回归用例（红）→ 修复 → gate+ctest（绿）→ 提交 → 下一项。
2. 每轮追加 `reports/nightly_report_waveform.md`：时间、修复内容、根因模式、测试证据、剩余清单。
3. 无法修复：不硬改、不删测试绕过；记「阻塞项」继续下一项。
4. 卡死难定位：offscreen 最小化复现 + `qDebug` 计时锁定阻塞段；**禁止加 sleep 掩盖**。
5. 收尾：§11 逐项打勾 → 最终报告 → 提交并 push（`git push origin domain/waveform`）。

---

## 9. 完成定义（叠加 CLAUDE.md §10 D1-D8）

| # | 检查项 | 判定方法 |
|---|---|---|
| N1 | Phase 1 清单全部闭环（修复或标注移交） | `reports/audit_waveform.md` |
| N2 | 每条缺陷有回归测试且全绿 | `ctest --output-on-failure` |
| N3 | 高频压力测试 3 种子 ×500 次操作零卡死零崩溃 | stress ctest 输出 |
| N4 | 看门狗全程零触发（300ms） | 压力测试日志 |
| N5 | SYM-1/2 有专门回归用例且通过 | `ctest -N` |
| N6 | 最终报告入库并 push | `reports/nightly_report_waveform.md` + git log |

---

## 10. 禁止事项

1. 禁止跳过审计直接改代码；禁止打补丁模式。
2. 禁止修改 master-owned 红线文件。
3. 禁止删除/跳过/弱化既有测试换绿灯。
4. 禁止用 `sleep`/`qWait` 掩盖时序问题。
5. 禁止本分支直接合入 master（由集成代理在主 checkout 执行）。
6. 禁止提交构建产物。

---

## 11. 验收矩阵

| 编号 | 验收项 | 证据位置 |
|---|---|---|
| A1 | Phase 0 基线报告 | reports/nightly_baseline_waveform.md |
| A2 | 缺陷审计清单 | reports/audit_waveform.md |
| A3 | 回归用例修复前红/修复后绿 | ctest 输出 + git 提交 |
| A4 | 统一修复完成 | git log（[波形] 提交） |
| A5 | 压力测试 3 种子全绿 | ctest 输出 |
| A6 | gate.bat 全绿 | gate 日志 |
| A7 | 最终报告 + push | reports/nightly_report_waveform.md、git ls-remote |

---

*文档结束。执行 agent 开工前先通读本文件与共享规格；天亮时以 §11 矩阵和最终报告验收。*
